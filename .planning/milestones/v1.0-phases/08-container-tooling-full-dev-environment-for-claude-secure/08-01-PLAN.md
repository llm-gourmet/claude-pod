---
phase: 08-container-tooling-full-dev-environment-for-claude-secure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - claude/Dockerfile
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
  - TOOL-04

must_haves:
  truths:
    - "git is available in the claude container"
    - "build-essential (gcc, make, etc.) is available in the claude container"
    - "python3, pip, and venv are available in the claude container"
    - "ripgrep (rg) and fd-find (fdfind) are available in the claude container"
    - "wget, openssh-client, and ca-certificates are available in the claude container"
    - "Existing tools (curl, jq, uuid-runtime, dnsutils) still work"
    - "Claude Code still installs and runs correctly"
    - "Non-root claude user is preserved with correct permissions"
  artifacts:
    - path: "claude/Dockerfile"
      provides: "Claude container with full dev environment"
      contains: "build-essential"
  key_links:
    - from: "claude/Dockerfile"
      to: "docker-compose.yml"
      via: "build: ./claude"
      pattern: "build:.*\\./claude"
---

<objective>
Expand the Claude container Dockerfile from a minimal node:22-slim image with only curl/jq/uuid-runtime/dnsutils to a fully-equipped development environment with git, build-essential, Python ecosystem, and fast search tools.

Purpose: Claude Code running inside the container needs core dev tools to work productively on real projects. Currently missing git, compilers, Python, and search utilities.
Output: Updated claude/Dockerfile with all requested dev tools installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-container-tooling-full-dev-environment-for-claude-secure/08-CONTEXT.md
@claude/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dev tools to Claude container Dockerfile</name>
  <files>claude/Dockerfile</files>
  <read_first>claude/Dockerfile, .planning/phases/08-container-tooling-full-dev-environment-for-claude-secure/08-CONTEXT.md</read_first>
  <action>
Modify the existing `apt-get install` line in `claude/Dockerfile` (line 4) to add the following packages. Keep the existing `--no-install-recommends` flag and the `rm -rf /var/lib/apt/lists/*` cleanup pattern per D-04 (image size is not a concern, but no reason to keep the apt cache).

Add these packages to the existing apt-get install line (per D-01, D-02, D-03):

**Core dev tools (D-01):**
- git
- build-essential
- ca-certificates
- openssh-client
- wget

**Python ecosystem (D-02):**
- python3
- python3-pip
- python3-venv

**Fast search tools (D-03):**
- ripgrep
- fd-find

Per D-05 and D-06: Do NOT install vim, nano, less, tree, zip, net-tools, iputils-ping, iproute2, or strace.

The result should be a single RUN layer that does:
1. apt-get update
2. apt-get install -y --no-install-recommends with ALL packages (existing + new)
3. rm -rf /var/lib/apt/lists/*
4. npm install -g @anthropic-ai/claude-code

Keep the rest of the Dockerfile exactly as-is. The USER claude directive, hook setup, settings setup, onboarding flag, and env vars must not change.
  </action>
  <verify>
    <automated>cd /home/igor9000/claude-secure && grep -q 'build-essential' claude/Dockerfile && grep -q 'python3' claude/Dockerfile && grep -q 'ripgrep' claude/Dockerfile && grep -q 'fd-find' claude/Dockerfile && grep -q 'git' claude/Dockerfile && grep -q 'openssh-client' claude/Dockerfile && grep -q 'wget' claude/Dockerfile && grep -q 'ca-certificates' claude/Dockerfile && grep -q 'python3-pip' claude/Dockerfile && grep -q 'python3-venv' claude/Dockerfile && echo "ALL PACKAGES PRESENT"</automated>
  </verify>
  <acceptance_criteria>
- grep 'build-essential' claude/Dockerfile returns a match
- grep 'python3-pip' claude/Dockerfile returns a match
- grep 'python3-venv' claude/Dockerfile returns a match
- grep 'ripgrep' claude/Dockerfile returns a match
- grep 'fd-find' claude/Dockerfile returns a match
- grep 'git' claude/Dockerfile returns a match
- grep 'openssh-client' claude/Dockerfile returns a match
- grep 'wget' claude/Dockerfile returns a match
- grep 'ca-certificates' claude/Dockerfile returns a match
- grep 'USER claude' claude/Dockerfile returns a match (non-root user preserved)
- grep 'npm install -g @anthropic-ai/claude-code' claude/Dockerfile returns a match (Claude Code install preserved)
- The Dockerfile has exactly one apt-get update line (single layer)
- No vim, nano, less, tree, net-tools, iputils-ping, iproute2, or strace in the file
  </acceptance_criteria>
  <done>claude/Dockerfile contains all 10 new packages alongside the 4 existing ones in a single apt-get install layer, with no other changes to the file structure</done>
</task>

<task type="auto">
  <name>Task 2: Build image and verify all tools are available</name>
  <files>claude/Dockerfile</files>
  <read_first>claude/Dockerfile</read_first>
  <action>
Build the claude container image and verify every installed tool is accessible:

1. Run `docker compose build claude` from the project root to build the updated image
2. Run verification commands inside the container to confirm each tool exists:
   - `docker compose run --rm claude git --version`
   - `docker compose run --rm claude gcc --version`
   - `docker compose run --rm claude make --version`
   - `docker compose run --rm claude python3 --version`
   - `docker compose run --rm claude pip3 --version`
   - `docker compose run --rm claude python3 -m venv --help`
   - `docker compose run --rm claude rg --version`
   - `docker compose run --rm claude fdfind --version`
   - `docker compose run --rm claude wget --version`
   - `docker compose run --rm claude ssh -V`
   - `docker compose run --rm claude curl --version` (existing tool still works)
   - `docker compose run --rm claude jq --version` (existing tool still works)
   - `docker compose run --rm claude uuidgen --help` (existing tool still works)
   - `docker compose run --rm claude whoami` (should output "claude", not "root")

If any command fails, fix the Dockerfile and rebuild. The fd-find package on Debian installs the binary as `fdfind` (not `fd`) -- verify using `fdfind`.

Note: `docker compose run` may need `--no-deps` if proxy/validator aren't built. Use `docker compose run --rm --no-deps claude <command>` if dependency services cause issues.
  </action>
  <verify>
    <automated>cd /home/igor9000/claude-secure && docker compose build claude 2>&1 | tail -5 && docker compose run --rm --no-deps claude bash -c "git --version && gcc --version | head -1 && python3 --version && pip3 --version && rg --version && fdfind --version && wget --version 2>&1 | head -1 && ssh -V 2>&1 && curl --version | head -1 && jq --version && whoami" 2>&1</automated>
  </verify>
  <acceptance_criteria>
- docker compose build claude completes without error
- git --version outputs a version string
- gcc --version outputs a version string
- python3 --version outputs "Python 3.x.x"
- pip3 --version outputs a version string
- rg --version outputs a version string
- fdfind --version outputs a version string
- wget --version outputs a version string
- ssh -V outputs "OpenSSH_x.x"
- curl --version still works (existing tool)
- jq --version still works (existing tool)
- whoami outputs "claude" (non-root user preserved)
  </acceptance_criteria>
  <done>Docker image builds successfully and all 10 new tools plus existing tools are verified working inside the container as the non-root claude user</done>
</task>

</tasks>

<verification>
All dev tools (git, build-essential, ca-certificates, openssh-client, wget, python3, python3-pip, python3-venv, ripgrep, fd-find) are installed and functional in the Claude container. Existing tools and security model (non-root user, root-owned hooks/settings) are preserved.
</verification>

<success_criteria>
- claude/Dockerfile updated with all requested packages
- Docker image builds without errors
- All 10 new tools produce valid version output when run inside the container
- Existing tools (curl, jq, uuid-runtime, dnsutils) still work
- Container runs as non-root "claude" user
- No prohibited tools (vim, nano, net-tools, etc.) installed
</success_criteria>

<output>
After completion, create `.planning/phases/08-container-tooling-full-dev-environment-for-claude-secure/08-01-SUMMARY.md`
</output>
