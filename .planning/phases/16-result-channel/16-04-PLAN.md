---
phase: 16-result-channel
plan: 04
type: execute
wave: 2
depends_on: [16-03]
requirements: [OPS-01]
files_modified:
  - install.sh
  - README.md
autonomous: true
gap_closure: false
must_haves:
  truths:
    - "After running install.sh, /opt/claude-secure/webhook/report-templates/ contains all four default Phase 16 templates (issues-opened.md, issues-labeled.md, push.md, workflow_run-completed.md)"
    - "Reinstalling overwrites existing /opt/claude-secure/webhook/report-templates/*.md with latest shipped versions (D-12 always-refresh semantics, mirror of Phase 15 step 5b)"
    - "Template directory is mode 755, template files are mode 644"
    - "resolve_report_template from installed bin/claude-secure finds /opt/claude-secure/webhook/report-templates/<event>.md as the final fallback"
    - "README documents REPORT_REPO, REPORT_BRANCH, REPORT_PATH_PREFIX, REPORT_REPO_TOKEN setup steps"
    - "install.sh does NOT rm -rf the report-templates directory (D-12: always refresh files, never wipe — preserves any operator-added custom templates in the same dir)"
  artifacts:
    - path: install.sh
      provides: "Step 5c that copies webhook/report-templates/*.md to /opt/claude-secure/webhook/report-templates/"
      contains: "report-templates"
    - path: README.md
      provides: "Phase 16 operator docs: REPORT_REPO_TOKEN PAT flow, profile .env additions, audit log location"
      contains: "REPORT_REPO_TOKEN"
  key_links:
    - from: install.sh step 5c
      to: /opt/claude-secure/webhook/report-templates/
      via: "sudo cp $app_dir/webhook/report-templates/*.md"
      pattern: "report-templates"
    - from: resolve_report_template production fallback
      to: /opt/claude-secure/webhook/report-templates/
      via: "5th entry in the D-08 fallback chain"
      pattern: "/opt/claude-secure/webhook/report-templates"
---

<objective>
Wave 2: Wire the installer so Phase 16 report templates land in the production path `/opt/claude-secure/webhook/report-templates/`, then document the REPORT_REPO_TOKEN onboarding flow. This closes the loop: on a fresh host, `sudo bash install.sh` followed by a spawn with a configured profile produces a live push to GitHub.

This plan flips the last 2 tests from 16-VALIDATION.md that require the production path (test for resolve_report_template fallback chain end-to-end + template file presence after install).

Purpose: Make OPS-01 shippable end-to-end on a clean host.
Output: install.sh step 5c added (~10 LOC mirror of step 5b), README Phase 16 section (~40 lines), 2 remaining Phase 16 tests flipped green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/16-result-channel/16-CONTEXT.md
@.planning/phases/16-result-channel/16-RESEARCH.md
@.planning/phases/16-result-channel/16-VALIDATION.md
@install.sh
@README.md

<interfaces>
<!-- install.sh existing step 5b (the template we're mirroring): -->

# install.sh lines 348-356:
# 5b. Copy default prompt templates (D-12: always refresh — latest templates ship)
#   sudo mkdir -p /opt/claude-secure/webhook/templates
#   if [ -d "$app_dir/webhook/templates" ]; then
#     sudo cp "$app_dir/webhook/templates/"*.md /opt/claude-secure/webhook/templates/
#     sudo chmod 644 /opt/claude-secure/webhook/templates/*.md
#     log_info "Copied default templates to /opt/claude-secure/webhook/templates/"
#   else
#     log_warn "Source directory $app_dir/webhook/templates not found — skipping template copy"
#   fi

# The new step 5c is a structural clone with:
#   templates -> report-templates
#   logs message updated

<!-- resolve_report_template fallback chain (from 16-02):
  1. profile-specific: $CONFIG_DIR/profiles/$PROFILE/report-templates/<event>.md
  2. env override: $WEBHOOK_REPORT_TEMPLATES_DIR/<event>.md
  3. dev path: $APP_DIR/webhook/report-templates/<event>.md
  4. prod path: /opt/claude-secure/webhook/report-templates/<event>.md
  5. hard fail (event_type with no template)
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add install.sh step 5c — copy webhook/report-templates/*.md to /opt/claude-secure/webhook/report-templates/</name>
  <files>install.sh</files>
  <read_first>
    - install.sh lines 330-403 (install_webhook_service function — step 5b is the verbatim template for step 5c)
    - webhook/report-templates/ (verify the four default templates from 16-01 are present in the repo checkout)
    - .planning/phases/16-result-channel/16-CONTEXT.md D-08, D-12 (fallback chain + always-refresh)
    - bin/claude-secure (the resolve_report_template function added in 16-02, to confirm the production path it expects)
  </read_first>
  <behavior>
    - After running install.sh, `/opt/claude-secure/webhook/report-templates/` exists and contains `issues-opened.md`, `issues-labeled.md`, `push.md`, `workflow_run-completed.md` (the four Phase 16 defaults)
    - Reinstalling overwrites existing files with the shipped versions (latest-wins for default templates)
    - install.sh does NOT `rm -rf` the directory — if an operator has added a custom `pull_request-opened.md` alongside the defaults, it SURVIVES (cp overwrites only the files being copied)
    - Directory permissions 755, file permissions 644 (matches step 5b)
    - If `$app_dir/webhook/report-templates` is missing in the source checkout, installer logs a warning and SKIPS (matches step 5b graceful-degrade behavior, allowing older checkouts to still install without blowing up)
    - install.sh passes `bash -n install.sh` syntax check after the edit
  </behavior>
  <action>
    Open `install.sh` and locate step 5b (lines 348-356). Insert a new step 5c IMMEDIATELY AFTER step 5b and BEFORE step 6 (line 358). The new block is a structural clone of step 5b with `templates` replaced by `report-templates`:

    ```bash
      # 5c. Copy default Phase 16 report templates (D-08 + D-12: always refresh).
      # Mirrors step 5b for the report-templates directory used by resolve_report_template
      # as the final fallback in the D-08 chain. We cp individual files (never rm -rf)
      # so operator-added custom templates in the same dir survive reinstall.
      sudo mkdir -p /opt/claude-secure/webhook/report-templates
      if [ -d "$app_dir/webhook/report-templates" ]; then
        sudo cp "$app_dir/webhook/report-templates/"*.md /opt/claude-secure/webhook/report-templates/
        sudo chmod 644 /opt/claude-secure/webhook/report-templates/*.md
        sudo chmod 755 /opt/claude-secure/webhook/report-templates
        log_info "Copied default report templates to /opt/claude-secure/webhook/report-templates/"
      else
        log_warn "Source directory $app_dir/webhook/report-templates not found -- skipping report template copy"
      fi
    ```

    Do NOT modify any other step. Do NOT change step 5b. Do NOT touch the systemd unit installation or the config template copy.

    Reasoning for the design:
    - `mkdir -p` first (idempotent, no error if exists)
    - `if [ -d "$app_dir/webhook/report-templates" ]` guards against older repo checkouts that predate Phase 16 (matches the step 5b pattern and avoids a hard failure during bisect/rollback scenarios)
    - `sudo cp "...*.md"` overwrites defaults but does NOT touch other files — custom operator-added templates like `pull_request-opened.md` are preserved
    - `sudo chmod 644` on files and `755` on the directory mirrors step 5b exactly (explicit dir chmod added because a fresh `mkdir -p` under sudo can produce umask-dependent permissions)
    - Log message uses the same `log_info` / `log_warn` convention as surrounding steps
  </action>
  <verify>
    <automated>bash -n install.sh && grep -c '/opt/claude-secure/webhook/report-templates' install.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n install.sh` exits 0 (syntax valid)
    - `grep -c '/opt/claude-secure/webhook/report-templates' install.sh` returns >= 3 (mkdir target + cp destination + chmod target + log message)
    - `grep -c '# 5c' install.sh` returns 1
    - `grep -c 'report-templates' install.sh` returns >= 4
    - `grep -cE 'rm -rf[^\n]*report-templates' install.sh` returns 0 (NEVER wipes the directory — D-12 always-refresh semantics only apply to individual file overwrites)
    - `grep -c 'Copied default report templates' install.sh` returns 1 (log line present)
    - Step 5c appears AFTER step 5b and BEFORE step 6 in install.sh (line-order verified with `grep -n '# 5[bc6]' install.sh | sort -n`)
    - No changes to step 5b or step 6 (diff shows only an insertion, no modifications elsewhere)
    - Smoke test (requires sudo): after running `sudo bash install.sh` against a scratch `/opt/claude-secure`, the files `issues-opened.md`, `issues-labeled.md`, `push.md`, `workflow_run-completed.md` exist under `/opt/claude-secure/webhook/report-templates/` with mode 644
    - Phase 16 test `test_install_report_templates` (if added in this task) flips GREEN using a sandboxed `$DESTDIR` harness
  </acceptance_criteria>
  <done>install.sh step 5c added; copies repo `webhook/report-templates/*.md` to `/opt/claude-secure/webhook/report-templates/` on every install/reinstall, preserves custom operator templates, enforces 644/755 permissions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update README with Phase 16 operator documentation (REPORT_REPO_TOKEN, audit log location, template customization)</name>
  <files>README.md</files>
  <read_first>
    - README.md (existing structure and heading conventions)
    - .planning/phases/16-result-channel/16-CONTEXT.md D-01..D-18 (the exact operator-facing configuration surface)
    - .planning/phases/16-result-channel/16-RESEARCH.md (skim the Pattern A section for the onboarding flow reference)
  </read_first>
  <behavior>
    - README gains a "Phase 16 / Result Channel" section (or extends an existing Headless Agent section) describing:
      * How to create a dedicated documentation repo for reports
      * How to provision a GitHub PAT with the minimum scopes (contents:write on the doc repo)
      * Where to add `REPORT_REPO_TOKEN` (profile .env file, NOT global)
      * Profile.json fields: `report_repo`, `report_branch`, `report_path_prefix`
      * Audit log location: `~/.claude-secure/logs/executions.jsonl`
      * How to jq-filter the audit log (example: `jq 'select(.status == "success")' executions.jsonl`)
      * How to customize report templates (profile-scope override path + prod fallback path)
      * `--skip-report` flag and `CLAUDE_SECURE_SKIP_REPORT` env var
      * Security notes: PAT never in URL, secrets from profile .env auto-redacted in committed reports
    - Copy-pasteable shell examples where relevant (profile .env addition, audit log tail one-liner)
    - README continues to render cleanly (no broken markdown, headings increment)
  </behavior>
  <action>
    Add a new section to README.md titled "## Phase 16 — Result Channel (Report Push + Audit Log)". Insert it AFTER the existing Phase 15 / Headless Agent section (or BEFORE the Security / Architecture section if that's the current layout — match surrounding markdown style).

    Content outline (expand each bullet into 1-3 sentences of prose; no need to dump all 18 decisions):

    ```markdown
    ## Phase 16 — Result Channel (Report Push + Audit Log)

    Every headless execution of `claude-secure spawn` produces two artifacts:

    1. A **structured markdown report** pushed to a dedicated documentation repo
    2. A **JSONL audit line** appended to `$LOG_DIR/executions.jsonl`

    ### One-time setup: documentation repo + PAT

    1. Create a new, empty GitHub repo for reports (e.g., `you/claude-reports`).
       Initialize with a `main` branch.
    2. Create a fine-grained GitHub PAT with the minimum scope: `contents: write`
       on the report repo ONLY. Do NOT grant scope on any source repo.
    3. Add the PAT and repo URL to the PROFILE .env (never the global .env):

       ```bash
       # ~/.claude-secure/profiles/<name>/.env
       REPORT_REPO_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
       ```

    4. Configure the profile JSON:

       ```bash
       jq '.report_repo = "https://github.com/you/claude-reports.git"
           | .report_branch = "main"
           | .report_path_prefix = "reports"' \
         ~/.claude-secure/profiles/<name>/profile.json > /tmp/p.json \
         && mv /tmp/p.json ~/.claude-secure/profiles/<name>/profile.json
       ```

    ### Audit log

    Every spawn appends one JSONL line to `$LOG_DIR/executions.jsonl`.
    Example filters:

    ```bash
    # tail recent successes
    tail -f ~/.claude-secure/logs/executions.jsonl | jq 'select(.status == "success")'

    # find push failures
    jq 'select(.status == "report_push_failed")' ~/.claude-secure/logs/executions.jsonl

    # total cost by profile
    jq -s 'group_by(.profile) | map({profile: .[0].profile, total_cost: (map(.cost_usd // 0) | add)})' \
      ~/.claude-secure/logs/executions.jsonl
    ```

    Audit lines are **always** written, regardless of whether the report push
    succeeded. `status=success` when the report pushed (or no report repo was
    configured), `status=report_push_failed` when the push failed (spawn still
    exits 0 — push failures are non-fatal), `status=claude_error` when Claude
    itself failed, `status=spawn_error` for pre-Claude failures.

    ### Customizing report templates

    Report templates are resolved in this order:

    1. `~/.claude-secure/profiles/<name>/report-templates/<event>.md` (profile override)
    2. `$WEBHOOK_REPORT_TEMPLATES_DIR/<event>.md` (env override, mostly for tests)
    3. `<repo-checkout>/webhook/report-templates/<event>.md` (dev)
    4. `/opt/claude-secure/webhook/report-templates/<event>.md` (production defaults shipped by installer)

    Default templates live in `webhook/report-templates/` in this repo and are
    copied to `/opt/claude-secure/webhook/report-templates/` on every install.
    To customize, drop a file of the same name in your profile's
    `report-templates/` directory — it takes precedence.

    Available variables: all Phase 15 D-16 vars (ISSUE_TITLE, ISSUE_BODY, REPO,
    COMMIT_SHA, etc.) plus Phase 16 extensions — RESULT_TEXT, ERROR_MESSAGE,
    COST_USD, DURATION_MS, SESSION_ID, TIMESTAMP, STATUS.

    ### Skipping publish (local-only runs)

    Pass `--skip-report` on the command line OR set `CLAUDE_SECURE_SKIP_REPORT=1`
    in the environment. The audit log entry is still written, but no clone or
    push is attempted. Useful for debugging and offline work.

    ### Security notes

    - The PAT is NEVER placed in the remote URL or argv. `git` is invoked with
      a `GIT_ASKPASS` helper that reads the PAT from an environment variable
      passed only to the child `git` process.
    - Every value from the profile `.env` file is automatically redacted from
      the rendered report body BEFORE committing — if your Claude output
      accidentally echoes `REPORT_REPO_TOKEN`, it is replaced with
      `<REDACTED:REPORT_REPO_TOKEN>` in the committed markdown.
    - Force-push is never used. If a concurrent run pushes first, we rebase
      and retry exactly once.
    - Reports are bounded at 16KB of Claude result text (larger outputs are
      truncated with a `... [truncated N more bytes]` marker).
    ```

    Keep the section self-contained and copy-pasteable. Do NOT reference decisions by ID (D-01, D-12) in user-facing docs — convert to natural prose.

    Do NOT rewrite any existing README sections. Insert the new section in the correct place based on the current table of contents.
  </action>
  <verify>
    <automated>bash -c 'test -f README.md && grep -c "Phase 16" README.md | grep -q "^[1-9]" && grep -c "REPORT_REPO_TOKEN" README.md | grep -q "^[1-9]" && grep -c "executions.jsonl" README.md | grep -q "^[1-9]" && grep -c "skip-report" README.md | grep -q "^[1-9]"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Phase 16' README.md` returns >= 1 (new section heading or subheading)
    - `grep -c 'REPORT_REPO_TOKEN' README.md` returns >= 2 (env var section + security notes)
    - `grep -c 'executions.jsonl' README.md` returns >= 2
    - `grep -c 'report_repo' README.md` returns >= 1 (profile.json example)
    - `grep -c 'skip-report\|CLAUDE_SECURE_SKIP_REPORT' README.md` returns >= 2
    - `grep -c 'GIT_ASKPASS' README.md` returns >= 1 (security notes)
    - `grep -c 'force-push\|force push' README.md` returns >= 1 (security notes)
    - `grep -c 'contents: write' README.md` returns >= 1 (PAT scope guidance)
    - `grep -c 'REDACTED' README.md` returns >= 1 (redaction callout)
    - Markdown renders: `bash -c 'command -v markdownlint-cli2 && markdownlint-cli2 README.md || true'` does not fatally error (best-effort — markdownlint may not be installed)
    - New section does NOT contain raw decision IDs like `D-01`, `D-12` (user-facing prose only): `grep -cE 'D-[0-9]{2}' README.md` returns 0 OR is unchanged from pre-task count
    - No existing README section was deleted or significantly rewritten (diff shows net additions only, plus possibly a single TOC insertion line)
  </acceptance_criteria>
  <done>README.md has a self-contained Phase 16 Result Channel section covering setup, audit log, template customization, --skip-report, and security notes with copy-pasteable examples.</done>
</task>

</tasks>

<verification>
After both tasks:

1. `bash -n install.sh` exits 0
2. `bash tests/test-phase16.sh` — all 30 tests PASS (the last 2 installer-dependent tests flip green)
3. `bash tests/test-phase13.sh && bash tests/test-phase14.sh && bash tests/test-phase15.sh` — zero failures
4. Static checks:
   - `grep -c 'report-templates' install.sh` >= 4
   - `grep -cE 'rm -rf[^\n]*report-templates' install.sh` == 0
   - README mentions REPORT_REPO_TOKEN, executions.jsonl, --skip-report, GIT_ASKPASS
5. End-to-end dry-run on a scratch host (manual-only, documented in 16-VALIDATION.md):
   - `sudo bash install.sh` places all four templates in `/opt/claude-secure/webhook/report-templates/`
   - Creating a profile with REPORT_REPO pointing at a live GitHub doc repo and invoking `claude-secure spawn` produces a commit in that repo visible via `git log`
</verification>

<success_criteria>
- Phase 16 all 30 tests green
- install.sh ships report templates to production path
- README documents operator onboarding end-to-end
- Zero regressions in Phase 13/14/15
- OPS-01 + OPS-02 shippable on a clean host with only the documented setup
</success_criteria>

<output>
After completion, create `.planning/phases/16-result-channel/16-04-SUMMARY.md` documenting:
- install.sh step 5c addition (line range)
- README Phase 16 section (line range, word count)
- Final Phase 16 test pass count (30/30 expected)
- Any deviations from the Wave 2 plan
- Confirmation that no Phase 13/14/15 regressions introduced
</output>
