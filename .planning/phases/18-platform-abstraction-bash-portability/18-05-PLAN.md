---
phase: 18-platform-abstraction-bash-portability
plan: 05
type: execute
wave: 4
depends_on: [18-02, 18-03, 18-04]
files_modified:
  - install.sh
  - tests/test-phase18.sh
autonomous: true
requirements: [PORT-01, PORT-02, TEST-01]
must_haves:
  truths:
    - "install.sh has the standard Phase 18 prologue: bash 4+ re-exec guard before any bash 4+ syntax, then sources lib/platform.sh, then calls claude_secure_bootstrap_path"
    - "Running CLAUDE_SECURE_PLATFORM_OVERRIDE=macos bash tests/test-phase18.sh on Linux CI exercises the macOS branch of detect_platform end-to-end and exits 0"
    - "The full Phase 18 test selection (bash run-tests.sh test-phase18.sh) is green"
    - "The legacy_detect_platform function in install.sh has been removed because the lib/platform.sh detect_platform has now fully replaced it"
    - "main() in install.sh calls the lib/platform.sh detect_platform (not the inline legacy version) and stores the result in PLATFORM"
  artifacts:
    - path: "install.sh"
      provides: "Phase 18 prologue (re-exec + lib source + bootstrap), legacy_detect_platform removed, main() routes through lib/platform.sh detect_platform"
      contains: "BASH_VERSINFO"
    - path: "tests/test-phase18.sh"
      provides: "test_phase18_full_suite_under_macos_override (replacing the Plan 01 stub) — re-runs the suite under CLAUDE_SECURE_PLATFORM_OVERRIDE=macos as a sub-suite"
      contains: "test_phase18_full_suite_under_macos_override"
  key_links:
    - from: "install.sh prologue"
      to: "lib/platform.sh"
      via: "source $SCRIPT_DIR/lib/platform.sh after re-exec guard"
      pattern: "source.*lib/platform.sh"
    - from: "install.sh main()"
      to: "lib/platform.sh detect_platform"
      via: "PLATFORM=$(detect_platform)"
      pattern: "PLATFORM=.*detect_platform"
---

<objective>
Final wiring + closure for Phase 18. This plan does three things:

1. Adds the bash 4+ re-exec guard prologue to install.sh (deferred from Plan 02 to avoid a files_modified conflict, since Plan 02 also touched install.sh for PLAT-03/PLAT-04). After this plan, install.sh has the same prologue as bin/claude-secure and run-tests.sh — re-exec into brew bash 5 on Apple bash 3.2, source lib/platform.sh, call claude_secure_bootstrap_path.

2. Removes the now-orphaned legacy_detect_platform function (Plan 02 renamed it as a transitional measure) and routes install.sh's main() through the lib/platform.sh detect_platform instead. The result becomes the single global PLATFORM value used by config.sh and downstream.

3. Implements the final test_phase18_full_suite_under_macos_override test in tests/test-phase18.sh, which proves TEST-01 end-to-end: re-running the full Phase 18 unit-test suite under CLAUDE_SECURE_PLATFORM_OVERRIDE=macos exits 0. This is the contract that Linux CI exercises macOS code paths without a Mac runner.

Purpose: Plans 02-04 each closed one or two requirements but left install.sh without a re-exec guard and without the final TEST-01 end-to-end assertion. This plan closes both gaps. It is intentionally last in the wave order so it can sequence after Plans 02 (install.sh patches), 03 (run-tests.sh prologue + bin prologue), and 04 (test-phase18.sh stubs replaced) without touching the same files concurrently.

Output: install.sh with the Phase 18 prologue, no legacy_detect_platform function, main() using lib/platform.sh detect_platform; tests/test-phase18.sh with the final TEST-01 stub replaced by a real subshell-based sub-suite invocation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-platform-abstraction-bash-portability/18-RESEARCH.md
@.planning/phases/18-platform-abstraction-bash-portability/18-02-PLAN.md
@.planning/phases/18-platform-abstraction-bash-portability/18-03-PLAN.md
@.planning/phases/18-platform-abstraction-bash-portability/18-04-PLAN.md
@.planning/phases/18-platform-abstraction-bash-portability/18-VALIDATION.md

<interfaces>
<!-- After Plan 02, install.sh top is: -->
#!/bin/bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/lib/platform.sh"
_invoking_user="${SUDO_USER:-$USER}"
...

<!-- And install.sh has legacy_detect_platform function (renamed from detect_platform in Plan 02). -->
<!-- main() currently calls legacy_detect_platform after check_dependencies. -->

<!-- The legacy_detect_platform function (Plan 02 just renamed it from detect_platform): -->
legacy_detect_platform() {
  if grep -qi microsoft /proc/version 2>/dev/null; then
    PLATFORM="wsl2"
    log_info "Detected WSL2 environment"
    local os_info
    os_info=$(docker info --format '{{.OperatingSystem}}' 2>/dev/null || echo "unknown")
    if echo "$os_info" | grep -qi "docker desktop"; then
      log_warn "Docker Desktop detected. iptables may not work correctly."
      log_warn "Recommended: use Docker CE installed directly in WSL2."
    fi
    local ipt_version
    ipt_version=$(iptables -V 2>/dev/null || echo "not found")
    log_info "iptables version: $ipt_version"
  else
    PLATFORM="linux"
    log_info "Detected native Linux environment"
  fi
  log_info "Detected platform: $PLATFORM"
}

<!-- This function does TWO things: (a) detect platform, (b) log Docker Desktop / iptables warnings on WSL2. -->
<!-- The lib/platform.sh detect_platform only does (a). So Plan 05's main() replacement must preserve the WSL2/Docker Desktop logging. -->

<!-- From lib/platform.sh (Plan 01): detect_platform returns linux | wsl2 | macos via uname -s + /proc/version check (same logic the inline version uses). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 18 prologue to install.sh and remove legacy_detect_platform</name>
  <files>install.sh</files>
  <read_first>
    - install.sh (full file after Plan 02 — confirm legacy_detect_platform exists, source lib/platform.sh line is at top, __INSTALL_SOURCE_ONLY guard at bottom)
    - bin/claude-secure first 50 lines (Plan 03 prologue — copy the exact same pattern)
    - lib/platform.sh detect_platform function (the replacement for legacy_detect_platform)
  </read_first>
  <behavior>
    - install.sh first lines re-exec into brew bash 5 on Apple bash 3.2.
    - After re-exec guard, set -euo pipefail runs.
    - source "$SCRIPT_DIR/lib/platform.sh" happens after the prologue.
    - claude_secure_bootstrap_path is called immediately so plain date, realpath, readlink resolve to GNU on macOS.
    - The legacy_detect_platform function is REMOVED entirely.
    - main() now does PLATFORM="$(detect_platform)"; log_info "Detected platform: $PLATFORM" and on wsl2 performs the Docker Desktop check + iptables version log inline (preserving the warnings the legacy function used to emit).
    - __INSTALL_SOURCE_ONLY=1 source guard at end of file remains intact.
  </behavior>
  <action>
    Edit install.sh with these EXACT changes:

    STEP 1 — REPLACE the first 6 lines of install.sh (the current `#!/bin/bash`, `set -euo pipefail`, blank, `SCRIPT_DIR=...`, `source "$SCRIPT_DIR/lib/platform.sh"`, `_invoking_user=...`) with this prologue block. The new top of the file:

    ```
    #!/usr/bin/env bash
    # Phase 18 PORT-02: bash 4+ re-exec guard. Apple ships bash 3.2.57 forever;
    # we re-exec into brew bash 5 so the rest of this script can use bash 4+ idioms.
    # This block MUST remain bash 3.2 safe (no double-bracket [[, no var lowercasing, no declare -A).
    if [ "${BASH_VERSINFO[0]:-0}" -lt 4 ]; then
      if command -v brew >/dev/null 2>&1; then
        __brew_bash="$(brew --prefix 2>/dev/null)/bin/bash"
        if [ -x "$__brew_bash" ]; then
          exec "$__brew_bash" "$0" "$@"
        fi
      fi
      echo "ERROR: bash 4+ required. On macOS run: brew install bash" >&2
      exit 1
    fi

    set -euo pipefail

    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    # shellcheck source=lib/platform.sh
    source "$SCRIPT_DIR/lib/platform.sh"
    # Phase 18 PORT-01: prepend gnubin to PATH so plain date/stat/readlink/realpath
    # resolve to GNU coreutils on macOS. No-op on Linux/WSL2.
    if command -v claude_secure_bootstrap_path >/dev/null 2>&1; then
      claude_secure_bootstrap_path || true
    fi

    _invoking_user="${SUDO_USER:-$USER}"
    ```

    Verify ordering after this edit: the BASH_VERSINFO line MUST appear before the `set -euo pipefail` line. Confirm with `grep -n` or the test in Task 2.

    STEP 2 — DELETE the entire legacy_detect_platform() function. Search for the line beginning with `legacy_detect_platform()` and remove every line through the matching closing `}` (inclusive). The function body is approximately 24 lines long. Do not leave any orphaned comments above it that referenced "Detect platform" — clean removal.

    STEP 3 — EDIT main() to replace the legacy_detect_platform call. main() currently (after Plan 02) contains a line `legacy_detect_platform` between `check_dependencies` and `check_existing`. REPLACE that single line with this 12-line block:

    ```
      PLATFORM="$(detect_platform)"
      log_info "Detected platform: $PLATFORM"
      if [ "$PLATFORM" = "wsl2" ]; then
        # Preserved from legacy_detect_platform: Docker Desktop + iptables warning on WSL2
        local os_info
        os_info=$(docker info --format '{{.OperatingSystem}}' 2>/dev/null || echo "unknown")
        if echo "$os_info" | grep -qi "docker desktop"; then
          log_warn "Docker Desktop detected. iptables may not work correctly."
          log_warn "Recommended: use Docker CE installed directly in WSL2."
        fi
        local ipt_version
        ipt_version=$(iptables -V 2>/dev/null || echo "not found")
        log_info "iptables version: $ipt_version"
      fi
    ```

    NOTE: `local` only works inside a function. main() IS a function in install.sh, so `local os_info` and `local ipt_version` are valid.

    STEP 4 — Confirm the bottom of install.sh still has the source-only guard from Plan 02:
    ```
    if [[ "${BASH_SOURCE[0]}" == "${0}" ]] && [ "${__INSTALL_SOURCE_ONLY:-0}" != "1" ]; then
      main "$@"
    fi
    ```
    If absent (Plan 02 was supposed to add it), re-add it now. Do not modify if already present.

    STEP 5 — Sanity check: `bash -n install.sh` must exit 0. Sourcing under `__INSTALL_SOURCE_ONLY=1` must still work.
  </action>
  <verify>
    <automated>bash tests/test-phase18.sh</automated>
  </verify>
  <acceptance_criteria>
    - install.sh line 1 is `#!/usr/bin/env bash`
    - install.sh first 30 lines contain `BASH_VERSINFO[0]:-0`
    - install.sh first 30 lines contain `command -v brew`
    - install.sh first 30 lines contain `exec "$__brew_bash" "$0" "$@"`
    - install.sh first 30 lines contain literal string `bash 4+ required. On macOS run: brew install bash`
    - install.sh first 40 lines contain `source "$SCRIPT_DIR/lib/platform.sh"`
    - install.sh first 40 lines contain `claude_secure_bootstrap_path`
    - install.sh re-exec guard line number is LESS than the `set -euo pipefail` line number (verify with grep -n on both patterns)
    - install.sh does NOT contain a function definition `legacy_detect_platform()` (verify with `! grep -q '^legacy_detect_platform()' install.sh`)
    - install.sh does NOT call `legacy_detect_platform` anywhere (verify with `! grep -q 'legacy_detect_platform' install.sh`)
    - install.sh main() body contains `PLATFORM="$(detect_platform)"`
    - install.sh main() body contains `Docker Desktop detected. iptables may not work correctly.` (preserved warning)
    - install.sh end-of-file guard contains `__INSTALL_SOURCE_ONLY`
    - `bash -n install.sh` exits 0
    - `__INSTALL_SOURCE_ONLY=1 bash -c 'source ./install.sh && type detect_platform && type macos_bootstrap_deps && type main'` succeeds and reports all three as functions
    - The added prologue uses NO `[[ ]]`, NO `${var,,}`, NO `declare -A` (`! sed -n '1,17p' install.sh | grep -E '\[\[|\$\{[A-Z_]+,,|declare -A'`)
  </acceptance_criteria>
  <done>
    install.sh has the Phase 18 prologue, legacy_detect_platform is removed, main() routes through lib/platform.sh detect_platform with WSL2 Docker Desktop warnings preserved inline, and __INSTALL_SOURCE_ONLY mode still works.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement test_phase18_full_suite_under_macos_override in tests/test-phase18.sh</name>
  <files>tests/test-phase18.sh</files>
  <read_first>
    - tests/test-phase18.sh (Plans 01-04 — see all the existing tests, the stub for test_phase18_full_suite_under_macos_override, and the run_test invocations)
    - install.sh (Task 1 just patched — confirm the prologue lets the file source under override)
    - lib/platform.sh (confirm detect_platform honors CLAUDE_SECURE_PLATFORM_OVERRIDE)
  </read_first>
  <behavior>
    test_phase18_full_suite_under_macos_override:
      - Re-invokes the same test-phase18.sh script in a subshell with CLAUDE_SECURE_PLATFORM_OVERRIDE=macos AND a sentinel env var __PHASE18_SUBSUITE=1 set so the subshell does NOT recursively run this same test (preventing infinite recursion).
      - Asserts the subshell exits 0.
      - Asserts the subshell output contains PASS lines for at least: test_detect_platform_override_macos, test_bootstrap_path_macos_with_fake_brew_succeeds, test_uuid_lower_normalizes (proving the macOS code paths were exercised under the override).
    The test's body must check the __PHASE18_SUBSUITE sentinel and skip itself (echo SKIP and return 0) when running inside the recursive invocation.
  </behavior>
  <action>
    Edit tests/test-phase18.sh with these EXACT changes:

    STEP 1 — REPLACE the stub `test_phase18_full_suite_under_macos_override()` with this real implementation:

    ```
    test_phase18_full_suite_under_macos_override() {
      # Recursion guard: when we are already inside the macOS-override sub-suite,
      # skip this test instead of recursing infinitely.
      if [ "${__PHASE18_SUBSUITE:-0}" = "1" ]; then
        echo "  (skipped inside __PHASE18_SUBSUITE=1)"
        return 0
      fi

      local subshell_log; subshell_log="$(mktemp)"
      local rc=0
      (
        export CLAUDE_SECURE_PLATFORM_OVERRIDE=macos
        export __PHASE18_SUBSUITE=1
        # Point the bootstrap at the fixture brew prefix so claude_secure_bootstrap_path
        # has a valid macOS environment to validate against.
        export CLAUDE_SECURE_BREW_PREFIX_OVERRIDE="$REPO_ROOT/tests/fixtures/brew"
        bash "$REPO_ROOT/tests/test-phase18.sh"
      ) > "$subshell_log" 2>&1 || rc=$?

      local log_contents
      log_contents="$(cat "$subshell_log")"
      rm -f "$subshell_log"

      if [ "$rc" -ne 0 ]; then
        echo "sub-suite under CLAUDE_SECURE_PLATFORM_OVERRIDE=macos exited $rc" >&2
        echo "$log_contents" >&2
        return 1
      fi

      # Spot-check that the macOS-relevant tests actually ran and passed in the sub-suite
      echo "$log_contents" | grep -q "PASS test_detect_platform_override_macos" || {
        echo "sub-suite missing PASS test_detect_platform_override_macos" >&2
        echo "$log_contents" >&2
        return 1
      }
      echo "$log_contents" | grep -q "PASS test_bootstrap_path_macos_with_fake_brew_succeeds" || {
        echo "sub-suite missing PASS test_bootstrap_path_macos_with_fake_brew_succeeds" >&2
        echo "$log_contents" >&2
        return 1
      }
      echo "$log_contents" | grep -q "PASS test_uuid_lower_normalizes" || {
        echo "sub-suite missing PASS test_uuid_lower_normalizes" >&2
        echo "$log_contents" >&2
        return 1
      }
      echo "$log_contents" | grep -q "FAILED=0" || {
        echo "sub-suite did not report FAILED=0" >&2
        echo "$log_contents" >&2
        return 1
      }
      return 0
    }
    ```

    The run_test invocation already exists from Plan 01. No other edits.

    STEP 2 — Verify there is no remaining `STUB: implemented` anywhere in tests/test-phase18.sh by running `grep -n "STUB: implemented" tests/test-phase18.sh` — it must return zero matches.
  </action>
  <verify>
    <automated>bash tests/test-phase18.sh</automated>
  </verify>
  <acceptance_criteria>
    - tests/test-phase18.sh `test_phase18_full_suite_under_macos_override()` body contains `CLAUDE_SECURE_PLATFORM_OVERRIDE=macos`
    - tests/test-phase18.sh `test_phase18_full_suite_under_macos_override()` body contains `__PHASE18_SUBSUITE=1`
    - tests/test-phase18.sh `test_phase18_full_suite_under_macos_override()` body contains the recursion guard (early return when `__PHASE18_SUBSUITE` is set)
    - tests/test-phase18.sh `test_phase18_full_suite_under_macos_override()` body grep-asserts `PASS test_detect_platform_override_macos`
    - tests/test-phase18.sh `test_phase18_full_suite_under_macos_override()` body grep-asserts `PASS test_bootstrap_path_macos_with_fake_brew_succeeds`
    - tests/test-phase18.sh `test_phase18_full_suite_under_macos_override()` body grep-asserts `FAILED=0`
    - tests/test-phase18.sh contains ZERO occurrences of `STUB: implemented` (verify with `! grep -q 'STUB: implemented' tests/test-phase18.sh`)
    - `bash tests/test-phase18.sh` exits 0
    - `bash tests/test-phase18.sh` output contains `PASS test_phase18_full_suite_under_macos_override`
    - `bash tests/test-phase18.sh` output contains `FAILED=0`
    - `CLAUDE_SECURE_PLATFORM_OVERRIDE=macos CLAUDE_SECURE_BREW_PREFIX_OVERRIDE="$(pwd)/tests/fixtures/brew" __PHASE18_SUBSUITE=1 bash tests/test-phase18.sh` exits 0 (the "outer" override invocation also passes — proves the recursion guard works)
    - `bash run-tests.sh test-phase18.sh` exits 0
  </acceptance_criteria>
  <done>
    test-phase18.sh has zero stubs left. test_phase18_full_suite_under_macos_override re-runs the suite under the macos override (with brew prefix override pointing at fixtures/brew) and asserts the macOS-relevant tests pass. Recursion guard prevents infinite loop. TEST-01 contract proven end-to-end on Linux CI.
  </done>
</task>

</tasks>

<verification>
1. `bash tests/test-phase18.sh` exits 0 with zero remaining stubs and PASS test_phase18_full_suite_under_macos_override.
2. `bash run-tests.sh test-phase18.sh` exits 0.
3. `bash run-tests.sh test-phase17.sh test-phase18.sh` exits 0 (cross-test-suite verification — Plan 04 already made test-phase17.sh green against the new mkdir lock).
4. `! grep -q 'legacy_detect_platform' install.sh` succeeds.
5. `! grep -q 'STUB: implemented' tests/test-phase18.sh` succeeds.
6. `bash -n install.sh` exits 0.
7. `__INSTALL_SOURCE_ONLY=1 bash -c 'source ./install.sh && type macos_bootstrap_deps && type detect_platform'` succeeds.
8. `! grep -nE '^[^#]*\bflock\b' install.sh bin/claude-secure run-tests.sh lib/platform.sh claude/hooks/pre-tool-use.sh` succeeds (final phase-wide flock audit).
</verification>

<success_criteria>
- install.sh has Phase 18 prologue and legacy_detect_platform is gone
- main() in install.sh routes through lib/platform.sh detect_platform with WSL2 Docker Desktop warnings preserved
- TEST-01 end-to-end: CLAUDE_SECURE_PLATFORM_OVERRIDE=macos sub-suite passes on Linux CI
- All Plan 01 stubs are now real tests (zero "STUB: implemented" strings remaining)
- Phase 18 quality gate: bash run-tests.sh test-phase17.sh test-phase18.sh exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/18-platform-abstraction-bash-portability/18-05-SUMMARY.md` documenting:
- Final state of install.sh (line counts, prologue position, legacy function removal)
- Confirmation that all 8 phase requirements (PLAT-02, PLAT-03, PLAT-04, PORT-01, PORT-02, PORT-03, PORT-04, TEST-01) have at least one passing test
- Manual-only verifications still outstanding from 18-VALIDATION.md (PLAT-03 brew bootstrap on real Mac, PORT-02 re-exec on real Apple bash 3.2) — these are intentionally deferred to Phase 19+ where real Mac hardware is required
- The cross-plan dependency chain that closed: 18-01 wrote tests asserting future state → 18-02/03/04 implemented the future state → 18-05 verified the final TEST-01 contract end-to-end
</output>
