---
phase: 15-event-handlers
plan: 02
type: execute
wave: 1
depends_on:
  - "15-01"
files_modified:
  - webhook/templates/issues-opened.md
  - webhook/templates/issues-labeled.md
  - webhook/templates/push.md
  - webhook/templates/workflow_run-completed.md
  - webhook/listener.py
autonomous: true
requirements:
  - HOOK-03
  - HOOK-04
  - HOOK-05
must_haves:
  truths:
    - "webhook/templates/ directory exists in source tree with 4 default .md files"
    - "issues-opened.md, issues-labeled.md, push.md, workflow_run-completed.md contain only the variables defined for their event type in D-16 (with {{REPO_NAME}} and {{EVENT_TYPE}} common)"
    - "webhook/listener.py has a new compute_event_type(headers, payload) helper that returns composite event type strings per D-01 (issues-opened, push, workflow_run-completed, ping)"
    - "webhook/listener.py has a new apply_event_filter(profile, event_type, payload) helper that returns (allowed: bool, reason: str) per D-05..D-10"
    - "Filter check is inserted between HMAC verification and payload persistence (AFTER HMAC, BEFORE persist_event) per D-07"
    - "Filtered events are logged with event=filtered and reason=<filter-name>, return 202, do NOT persist to disk, do NOT spawn per D-07"
    - "Accepted events are logged with event=routed immediately before spawn_async per D-23"
    - "persist_event() writes top-level event_type field to the persisted JSON, alongside (not replacing) the existing _meta.event_type per D-02"
    - "DEFAULT_FILTER dict encodes sane defaults: issues opened+labeled, push main+master, workflow_run failures per D-06"
    - "ping events are filtered with reason=unsupported_event:ping (no template lookup, no spawn) per Pitfall 4"
    - "Loop prevention (pusher.name in webhook_bot_users) runs BEFORE branch matching so a bot push to main is still filtered per D-09"
    - "Phase 14 test suite remains green (no regressions on HOOK-01/02/06)"
  artifacts:
    - path: "webhook/templates/issues-opened.md"
      provides: "Default Issue-opened prompt with brief context + action-oriented instructions"
      contains: "{{ISSUE_TITLE}}"
    - path: "webhook/templates/issues-labeled.md"
      provides: "Default Issue-labeled prompt with current label set"
      contains: "{{ISSUE_LABELS}}"
    - path: "webhook/templates/push.md"
      provides: "Default push prompt with branch + commit context"
      contains: "{{COMMIT_SHA}}"
    - path: "webhook/templates/workflow_run-completed.md"
      provides: "Default CI failure diagnosis prompt"
      contains: "{{WORKFLOW_NAME}}"
    - path: "webhook/listener.py"
      provides: "Event-aware dispatcher with compute_event_type, apply_event_filter, top-level event_type injection, filtered/routed logging"
      contains: "def compute_event_type"
  key_links:
    - from: "webhook/listener.py do_POST handler"
      to: "compute_event_type(headers, payload)"
      via: "post-HMAC, pre-persist call"
      pattern: "compute_event_type\\("
    - from: "webhook/listener.py do_POST handler"
      to: "apply_event_filter(profile, event_type, payload)"
      via: "filter check returning (allowed, reason)"
      pattern: "apply_event_filter\\("
    - from: "webhook/listener.py persist_event"
      to: "top-level event_type field in JSON"
      via: "payload['event_type'] = event_type"
      pattern: "payload\\[.event_type.\\]"
    - from: "webhook/listener.py filtered path"
      to: "webhook.jsonl event=filtered"
      via: "log_event(event='filtered', reason=...)"
      pattern: "event=.filtered"
    - from: "webhook/listener.py accepted path"
      to: "webhook.jsonl event=routed"
      via: "log_event(event='routed', ...)"
      pattern: "event=.routed"
---

<objective>
Create the four default prompt templates (`webhook/templates/*.md`) and extend `webhook/listener.py` with composite event-type derivation, per-profile filtering, ping handling, and top-level `event_type` injection into persisted event JSON.

Purpose: Deliver HOOK-03 (issues routing), HOOK-04 (push-to-main routing with loop prevention), and HOOK-05 (workflow_run failure routing). After this plan lands, the listener stops forwarding `ping` events, stops forwarding closed-issue / non-main-push / successful-workflow events, and writes enriched event files that spawn (Plan 15-03) will consume.

Output: A new `webhook/templates/` directory populated with 4 markdown files per D-11, and a modified `webhook/listener.py` with two new helpers and a reroute through the filter stage.

This plan does NOT touch `bin/claude-secure` (Plan 15-03's job) or `install.sh` (Plan 15-04's job).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-event-handlers/15-CONTEXT.md
@.planning/phases/15-event-handlers/15-RESEARCH.md
@.planning/phases/15-event-handlers/15-VALIDATION.md
@.planning/phases/14-webhook-listener/14-CONTEXT.md
@.planning/phases/14-webhook-listener/14-02-SUMMARY.md
@webhook/listener.py
@tests/test-phase15.sh
@tests/test-phase14.sh

<interfaces>
<!-- Existing listener.py symbols (do NOT regress): -->
<!-- persist_event(events_dir, raw_body, profile_name, event_type, delivery_id) -> pathlib.Path -->
<!-- resolve_profile_by_repo(profiles_dir, repo) -> dict | None -->
<!-- spawn_async(profile_name, event_path, delivery_id) -> None -->
<!-- log_event(**kwargs) -> None   (writes JSONL to webhook.jsonl) -->
<!-- WebhookHandler.do_POST() -> currently at line ~272; HMAC check at ~341; persist at ~361 -->

<!-- New symbols this plan adds: -->
<!-- compute_event_type(headers: dict, payload: dict) -> str -->
<!-- apply_event_filter(profile: dict, event_type: str, payload: dict) -> tuple[bool, str] -->
<!-- DEFAULT_FILTER: dict (module-level constant) -->

<!-- persist_event signature unchanged; internal body adds payload["event_type"] = event_type line -->
</interfaces>

<default_template_content>
See .planning/phases/15-event-handlers/15-RESEARCH.md lines 843-937 for the exact four-template content. Copy verbatim — tone and wording are locked by RESEARCH. Do NOT paraphrase or reorder sections.
</default_template_content>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend webhook/listener.py with compute_event_type, apply_event_filter, ping handling, top-level event_type injection, filtered/routed logging</name>
  <files>webhook/listener.py</files>
  <behavior>
    Test 1 (test_compute_event_type_cases): compute_event_type({'X-GitHub-Event':'issues'}, {'action':'opened'}) returns 'issues-opened'; ({'X-GitHub-Event':'push'}, {}) returns 'push'; ({'X-GitHub-Event':'ping'}, {}) returns 'ping'; ({'X-GitHub-Event':'workflow_run'}, {'action':'completed'}) returns 'workflow_run-completed'.

    Test 2 (test_issues_opened_routes): POST valid HMAC-signed github-issues-opened.json → 202, event file written, top-level .event_type=="issues-opened", log event=routed.

    Test 3 (test_issues_labeled_routes): same for github-issues-labeled.json → 202, event file, event_type=="issues-labeled".

    Test 4 (test_issues_closed_filtered): POST issues-closed payload (action=closed) → 202 but NO event file, NO spawn call; webhook.jsonl contains event=filtered with reason=issue_action_not_matched:closed.

    Test 5 (test_push_main_routes): github-push.json → 202, event file, event_type=="push".

    Test 6 (test_push_feature_branch_filtered): github-push-feature-branch.json (ref=refs/heads/feature/xyz) → 202, no event file, reason=branch_not_matched:feature/xyz.

    Test 7 (test_push_bot_loop_filtered): profile with webhook_bot_users=["claude-bot"], push from pusher.name=claude-bot to main → 202, no event file, reason=loop_prevention. Loop check BEFORE branch check.

    Test 8 (test_push_branch_delete_no_crash): github-push-branch-delete.json (head_commit=null, ref=refs/heads/old-feature) → filter naturally rejects (branch not in allow list) → 202, no crash.

    Test 9 (test_workflow_run_failure_routes): github-workflow-run-failure.json → 202, event file, event_type=="workflow_run-completed".

    Test 10 (test_workflow_run_success_filtered): github-workflow-run-success.json → 202, no event file, reason=workflow_conclusion_not_matched:success.

    Test 11 (test_workflow_run_in_progress_filtered): github-workflow-run-in-progress.json → 202, no event file, reason=workflow_action_not_completed:in_progress.

    Test 12 (test_ping_event_filtered): github-ping.json (no action, no issue; valid HMAC) → 202, no event file, reason=unsupported_event:ping.

    Test 13 (test_event_file_has_top_level_event_type): persisted JSON has BOTH .event_type="issues-opened" AND ._meta.event_type="issues-opened".

    Test 14 (regression): tests/test-phase14.sh full suite still green.
  </behavior>
  <action>
    Edit `webhook/listener.py` with the following changes. Do NOT touch HMAC logic, semaphore setup, ThreadingHTTPServer config, unit file references, response codes (except adding the 202 filtered path), port/bind config.

    ### Step A: Add module-level DEFAULT_FILTER constant (near the top, after imports)

    ```python
    # D-06: Sane defaults when profile.webhook_event_filter is omitted.
    # Empty arrays for labels/workflows mean "match anything in that category".
    DEFAULT_FILTER = {
        "issues": {"actions": ["opened", "labeled"], "labels": []},
        "push": {"branches": ["main", "master"]},
        "workflow_run": {"conclusions": ["failure"], "workflows": []},
    }
    ```

    ### Step B: Add compute_event_type() helper

    Insert as a module-level function above `persist_event`. Copy from 15-RESEARCH.md Pattern 1 verbatim:

    ```python
    def compute_event_type(headers: dict, payload: dict) -> str:
        """Collapse (X-GitHub-Event, payload.action) into composite type string.

        D-01: Examples:
            ('issues', 'opened')          -> 'issues-opened'
            ('issues', 'labeled')         -> 'issues-labeled'
            ('push', None)                -> 'push'
            ('workflow_run', 'completed') -> 'workflow_run-completed'
            ('ping', None)                -> 'ping'
        """
        base = (headers.get("X-GitHub-Event") or "").strip()
        if not base:
            return "unknown"
        action = payload.get("action") if isinstance(payload, dict) else None
        if isinstance(action, str) and action:
            return f"{base}-{action}"
        return base
    ```

    Note: `headers` may be a `BaseHTTPRequestHandler.headers` object which supports `.get()`. Pass it through directly; do NOT convert to dict.

    ### Step C: Add apply_event_filter() helper

    Insert below compute_event_type. Copy from 15-RESEARCH.md Pattern 2 verbatim (D-05 through D-10). Key invariants the code MUST express:

    1. `base = event_type.split("-", 1)[0]` — re-derive X-GitHub-Event base.
    2. Unknown base event types (not in DEFAULT_FILTER and not in profile.webhook_event_filter) return `(False, f"unsupported_event:{base}")`. This catches `ping` and any other event GitHub may add.
    3. Issues branch: filter on `action` then optionally on `labels` intersection.
    4. Push branch: loop_prevention FIRST (check `webhook_bot_users`), THEN branch exact-match strip of `refs/heads/` prefix. No glob.
    5. workflow_run branch: require `action == completed`, then filter by `workflow_run.conclusion`, then optionally by `workflow.name` / `workflow_run.name`.
    6. Filter takes the already-loaded profile dict — zero I/O, sub-millisecond (Pitfall 3).

    ### Step D: Modify persist_event() to inject top-level event_type

    At line ~157 of the existing function, after `payload = json.loads(raw_body)` and BEFORE `payload["_meta"] = {...}`, add:

    ```python
    payload["event_type"] = event_type           # D-02: canonical top-level field
    ```

    Keep the existing `_meta` block unchanged — `_meta.event_type` is retained for backward compatibility (D-02 explicit).

    ### Step E: Rewire do_POST() to call compute_event_type + apply_event_filter between HMAC and persist

    At line ~355 (immediately after the HMAC compare_digest success branch, before the existing `delivery_id = ...` and `event_type = self.headers.get(...)` lines), replace the header-only event_type extraction with:

    ```python
    # Persist payload + enqueue spawn
    delivery_id = self.headers.get(
        "X-GitHub-Delivery", f"nodelivery-{uuid.uuid4().hex[:8]}"
    )

    # D-01: composite event type from header + payload.action
    event_type = compute_event_type(self.headers, payload)

    # D-05..D-10: per-profile filter between HMAC and persist.
    # D-07: filtered events log + return 202 WITHOUT persisting or spawning.
    allowed, reason = apply_event_filter(profile, event_type, payload)
    if not allowed:
        log_event(
            event="filtered",
            profile=profile["name"],
            repo=repo,
            delivery_id=delivery_id,
            event_type=event_type,
            reason=reason,
            status_code=202,
        )
        return self._send_json(202, {"status": "filtered", "reason": reason})

    try:
        event_path = persist_event(
            _config.events_dir,
            raw_body,
            profile["name"],
            event_type,
            delivery_id,
        )
    except Exception as exc:
        log_event(
            event="rejected",
            profile=profile["name"],
            reason=f"persist_failed:{exc}",
            delivery_id=delivery_id,
            status_code=500,
        )
        return self._send_json(
            500, {"error": "persist_failed", "detail": str(exc)}
        )

    # D-23: new 'routed' log event — the event is accepted into the spawn pipeline
    log_event(
        event="routed",
        profile=profile["name"],
        repo=repo,
        delivery_id=delivery_id,
        event_type=event_type,
        status_code=202,
    )

    # Retain the existing 'received' log for backward compat with Phase 14 tests
    log_event(
        event="received",
        profile=profile["name"],
        repo=repo,
        delivery_id=delivery_id,
        event_type=event_type,
        status_code=202,
    )

    spawn_async(profile["name"], event_path, delivery_id)
    return self._send_json(202, {"status": "accepted", "delivery_id": delivery_id})
    ```

    IMPORTANT — backward compat:
    - The existing `log_event(event="received", ...)` from Phase 14 must be preserved (Phase 14 tests grep for `event=received` lines). Emit both `routed` and `received` for accepted events.
    - The return JSON body must stay `{"status": "accepted", ...}` for accepted events and add `{"status": "filtered", "reason": ...}` for filtered events. Phase 14 tests that check for 202 + any JSON body will still pass.

    ### Step F: Verify no regressions

    Before marking done, run `bash tests/test-phase14.sh` and confirm all tests still pass. If any Phase 14 test fails, revert and debug — the listener MUST remain backward compatible.

    ### Anti-patterns to avoid
    - Do NOT put the filter check before HMAC verification (Pitfall 2: security regression — never read payload fields for decision-making before signature validation).
    - Do NOT re-read profile.json from disk inside apply_event_filter (Pitfall 3: performance regression).
    - Do NOT extract a `webhook/filter.py` module — CONTEXT says single-file precedent wins for now (Open Question 3).
    - Do NOT remove or rename `event_type` in the `_meta` block (backward compat with pre-Phase-15 event files).
  </action>
  <verify>
    <automated>python3 -c "import sys; sys.path.insert(0, 'webhook'); from listener import compute_event_type, apply_event_filter, DEFAULT_FILTER; assert compute_event_type({'X-GitHub-Event':'issues'}, {'action':'opened'}) == 'issues-opened'; assert compute_event_type({'X-GitHub-Event':'push'}, {}) == 'push'; assert compute_event_type({'X-GitHub-Event':'ping'}, {}) == 'ping'; assert compute_event_type({'X-GitHub-Event':'workflow_run'}, {'action':'completed'}) == 'workflow_run-completed'; p = {'name':'t','webhook_event_filter':{}}; a,r = apply_event_filter(p, 'ping', {}); assert a is False and 'unsupported_event' in r; a,r = apply_event_filter({'name':'t'}, 'issues-opened', {'action':'opened','issue':{'labels':[]}}); assert a is True; a,r = apply_event_filter({'name':'t'}, 'issues-closed', {'action':'closed'}); assert a is False and 'issue_action_not_matched' in r; a,r = apply_event_filter({'name':'t'}, 'push', {'ref':'refs/heads/feature/x','pusher':{'name':'alice'}}); assert a is False and 'branch_not_matched' in r; a,r = apply_event_filter({'name':'t','webhook_bot_users':['bot']}, 'push', {'ref':'refs/heads/main','pusher':{'name':'bot'}}); assert a is False and 'loop_prevention' in r; a,r = apply_event_filter({'name':'t'}, 'workflow_run-completed', {'action':'completed','workflow_run':{'conclusion':'failure'}}); assert a is True; a,r = apply_event_filter({'name':'t'}, 'workflow_run-completed', {'action':'completed','workflow_run':{'conclusion':'success'}}); assert a is False and 'workflow_conclusion_not_matched' in r; a,r = apply_event_filter({'name':'t'}, 'workflow_run-in_progress', {'action':'in_progress','workflow_run':{'conclusion':None}}); assert a is False and 'workflow_action_not_completed' in r; print('listener unit OK')" && grep -q 'def compute_event_type' webhook/listener.py && grep -q 'def apply_event_filter' webhook/listener.py && grep -q 'DEFAULT_FILTER' webhook/listener.py && grep -q 'payload\["event_type"\]' webhook/listener.py && grep -q "event=.filtered\|event=\"filtered\"" webhook/listener.py && grep -q "event=.routed\|event=\"routed\"" webhook/listener.py && bash tests/test-phase14.sh && bash tests/test-phase15.sh test_compute_event_type_cases && bash tests/test-phase15.sh test_issues_opened_routes && bash tests/test-phase15.sh test_issues_closed_filtered && bash tests/test-phase15.sh test_push_main_routes && bash tests/test-phase15.sh test_push_feature_branch_filtered && bash tests/test-phase15.sh test_push_bot_loop_filtered && bash tests/test-phase15.sh test_push_branch_delete_no_crash && bash tests/test-phase15.sh test_workflow_run_failure_routes && bash tests/test-phase15.sh test_workflow_run_success_filtered && bash tests/test-phase15.sh test_workflow_run_in_progress_filtered && bash tests/test-phase15.sh test_ping_event_filtered && bash tests/test-phase15.sh test_event_file_has_top_level_event_type && bash tests/test-phase15.sh test_issues_labeled_routes</automated>
  </verify>
  <done>compute_event_type and apply_event_filter functions defined in webhook/listener.py; DEFAULT_FILTER dict present; persist_event injects top-level event_type; do_POST calls compute_event_type and apply_event_filter between HMAC and persist; filtered events log event=filtered and do NOT persist/spawn; accepted events log both event=routed (new) and event=received (Phase 14 backward compat); Phase 14 test suite fully green; all 13 listener-related Phase 15 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Create webhook/templates/ directory and 4 default prompt templates</name>
  <files>
    webhook/templates/issues-opened.md,
    webhook/templates/issues-labeled.md,
    webhook/templates/push.md,
    webhook/templates/workflow_run-completed.md
  </files>
  <action>
    Create a new directory `webhook/templates/` (sibling to `webhook/listener.py`) and populate it with the four default prompt templates. Content is LOCKED by 15-RESEARCH.md lines 847-937 — copy verbatim. Do NOT paraphrase, reorder sections, or "improve" the wording. The plan-checker will grep for specific variable placeholders.

    ### webhook/templates/issues-opened.md

    Exact content from RESEARCH (reproduced here for clarity):

    ```markdown
    A new GitHub issue was opened in {{REPO_NAME}}.

    **Issue #{{ISSUE_NUMBER}}:** {{ISSUE_TITLE}}
    **Author:** {{ISSUE_AUTHOR}}
    **URL:** {{ISSUE_URL}}
    **Labels:** {{ISSUE_LABELS}}

    **Body:**
    {{ISSUE_BODY}}

    ---

    Please:
    1. Read the issue carefully and check the repository README and any contributing guidelines.
    2. If the issue is a bug report with a reproducible failure, investigate the cause in the codebase.
    3. If it is a feature request, evaluate feasibility and scope.
    4. Post a substantive comment on the issue using `gh issue comment {{ISSUE_NUMBER}} --repo {{REPO_NAME}} --body "..."` that acknowledges the report, asks any clarifying questions, and outlines next steps.

    Do not make code changes in this session. Your output is a comment on the issue, not a patch.
    ```

    ### webhook/templates/issues-labeled.md

    Exact content from RESEARCH lines 870-890.

    ### webhook/templates/push.md

    Exact content from RESEARCH lines 892-914.

    ### webhook/templates/workflow_run-completed.md

    Exact content from RESEARCH lines 916-937.

    Variable set per D-16:
    - issues-opened / issues-labeled: {{REPO_NAME}}, {{ISSUE_NUMBER}}, {{ISSUE_TITLE}}, {{ISSUE_AUTHOR}}, {{ISSUE_URL}}, {{ISSUE_LABELS}}, {{ISSUE_BODY}}
    - push: {{REPO_NAME}}, {{BRANCH}}, {{COMMIT_SHA}}, {{COMMIT_AUTHOR}}, {{PUSHER}}, {{COMPARE_URL}}, {{COMMIT_MESSAGE}}
    - workflow_run-completed: {{REPO_NAME}}, {{WORKFLOW_NAME}}, {{WORKFLOW_RUN_ID}}, {{BRANCH}}, {{COMMIT_SHA}}, {{WORKFLOW_CONCLUSION}}, {{WORKFLOW_RUN_URL}}

    Templates must NOT reference variables outside their D-16 set (plan-checker will grep for foreign variables). The filter naturally ensures workflow_run-completed renders only on `conclusion=failure`, so the template can safely say "A CI workflow failed" without mentioning other conclusions.
  </action>
  <verify>
    <automated>test -d webhook/templates && test -f webhook/templates/issues-opened.md && test -f webhook/templates/issues-labeled.md && test -f webhook/templates/push.md && test -f webhook/templates/workflow_run-completed.md && grep -q '{{REPO_NAME}}' webhook/templates/issues-opened.md && grep -q '{{ISSUE_NUMBER}}' webhook/templates/issues-opened.md && grep -q '{{ISSUE_TITLE}}' webhook/templates/issues-opened.md && grep -q '{{ISSUE_AUTHOR}}' webhook/templates/issues-opened.md && grep -q '{{ISSUE_URL}}' webhook/templates/issues-opened.md && grep -q '{{ISSUE_LABELS}}' webhook/templates/issues-opened.md && grep -q '{{ISSUE_BODY}}' webhook/templates/issues-opened.md && grep -q '{{ISSUE_LABELS}}' webhook/templates/issues-labeled.md && grep -q '{{ISSUE_BODY}}' webhook/templates/issues-labeled.md && grep -q '{{REPO_NAME}}' webhook/templates/push.md && grep -q '{{BRANCH}}' webhook/templates/push.md && grep -q '{{COMMIT_SHA}}' webhook/templates/push.md && grep -q '{{COMMIT_AUTHOR}}' webhook/templates/push.md && grep -q '{{PUSHER}}' webhook/templates/push.md && grep -q '{{COMPARE_URL}}' webhook/templates/push.md && grep -q '{{COMMIT_MESSAGE}}' webhook/templates/push.md && grep -q '{{REPO_NAME}}' webhook/templates/workflow_run-completed.md && grep -q '{{WORKFLOW_NAME}}' webhook/templates/workflow_run-completed.md && grep -q '{{WORKFLOW_RUN_ID}}' webhook/templates/workflow_run-completed.md && grep -q '{{BRANCH}}' webhook/templates/workflow_run-completed.md && grep -q '{{COMMIT_SHA}}' webhook/templates/workflow_run-completed.md && grep -q '{{WORKFLOW_CONCLUSION}}' webhook/templates/workflow_run-completed.md && grep -q '{{WORKFLOW_RUN_URL}}' webhook/templates/workflow_run-completed.md && ! grep -q '{{ISSUE_TITLE}}' webhook/templates/push.md && ! grep -q '{{COMMIT_SHA}}' webhook/templates/issues-opened.md && ! grep -q '{{WORKFLOW_NAME}}' webhook/templates/push.md && bash tests/test-phase15.sh test_default_template_issues_opened_exists</automated>
  </verify>
  <done>webhook/templates/ directory exists with 4 .md files; each file contains only its D-16 variable set; cross-contamination grep (e.g. ISSUE_TITLE in push.md) returns nothing; the Wave 0 file-existence test goes green.</done>
</task>

</tasks>

<verification>
Full verification after both tasks:

  python3 -c "import sys; sys.path.insert(0, 'webhook'); from listener import compute_event_type, apply_event_filter, DEFAULT_FILTER; print('imports OK')"
  bash tests/test-phase14.sh                         # Phase 14 regression — MUST be green
  bash tests/test-phase15.sh test_compute_event_type_cases
  bash tests/test-phase15.sh test_issues_opened_routes
  bash tests/test-phase15.sh test_issues_labeled_routes
  bash tests/test-phase15.sh test_issues_closed_filtered
  bash tests/test-phase15.sh test_push_main_routes
  bash tests/test-phase15.sh test_push_feature_branch_filtered
  bash tests/test-phase15.sh test_push_bot_loop_filtered
  bash tests/test-phase15.sh test_push_branch_delete_no_crash
  bash tests/test-phase15.sh test_workflow_run_failure_routes
  bash tests/test-phase15.sh test_workflow_run_success_filtered
  bash tests/test-phase15.sh test_workflow_run_in_progress_filtered
  bash tests/test-phase15.sh test_ping_event_filtered
  bash tests/test-phase15.sh test_event_file_has_top_level_event_type
  bash tests/test-phase15.sh test_default_template_issues_opened_exists

Expected state after this plan: listener-side Phase 15 tests green; template-existence tests green; Phase 14 suite still fully green. Plan 15-03 is required to green the bin/claude-secure tests (render, resolve, replay, extract_payload_field).
</verification>

<success_criteria>
- [ ] `webhook/templates/` directory exists with 4 .md files matching RESEARCH content verbatim
- [ ] Templates use only their D-16 variable set (no cross-contamination)
- [ ] `webhook/listener.py` has `compute_event_type(headers, payload)` function per Pattern 1
- [ ] `webhook/listener.py` has `apply_event_filter(profile, event_type, payload)` function per Pattern 2
- [ ] `webhook/listener.py` has `DEFAULT_FILTER` module-level dict per D-06
- [ ] `persist_event()` injects top-level `event_type` field into persisted JSON per D-02
- [ ] `do_POST()` calls filter between HMAC and persist per D-07
- [ ] Filtered events log `event=filtered` + `reason=<name>` and return 202 without persisting or spawning
- [ ] Accepted events log `event=routed` (new) AND `event=received` (Phase 14 backward compat)
- [ ] Loop prevention runs BEFORE branch matching in push filter
- [ ] `ping` events are filtered with `reason=unsupported_event:ping`
- [ ] `bash tests/test-phase14.sh` fully green (zero regressions)
- [ ] All 13 listener-related Phase 15 tests green
- [ ] `test_default_template_issues_opened_exists` green
</success_criteria>

<output>
After completion, create `.planning/phases/15-event-handlers/15-02-SUMMARY.md` documenting:
- The four default templates created (path + variable set used)
- The two new listener helpers (compute_event_type, apply_event_filter) with signatures
- Where the filter check was inserted in do_POST (line reference)
- The top-level event_type injection in persist_event
- Confirmation that Phase 14 regression suite still passes
- Explicit note that bin/claude-secure is untouched — Plan 15-03 handles spawn-side changes
</output>
