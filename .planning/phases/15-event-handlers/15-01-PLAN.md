---
phase: 15-event-handlers
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test-phase15.sh
  - tests/fixtures/github-issues-labeled.json
  - tests/fixtures/github-push-feature-branch.json
  - tests/fixtures/github-push-bot-loop.json
  - tests/fixtures/github-push-branch-delete.json
  - tests/fixtures/github-workflow-run-failure.json
  - tests/fixtures/github-workflow-run-success.json
  - tests/fixtures/github-workflow-run-in-progress.json
  - tests/fixtures/github-ping.json
  - tests/fixtures/github-issues-opened-with-pipe.json
  - tests/test-map.json
autonomous: true
requirements:
  - HOOK-03
  - HOOK-04
  - HOOK-05
  - HOOK-07
must_haves:
  truths:
    - "tests/test-phase15.sh exists, is executable, and runs to completion (Wave 0: many tests FAIL until Plans 15-02/15-03/15-04 land — that is the Nyquist self-healing contract)"
    - "All 28 test functions from 15-VALIDATION.md Per-Task Verification Map exist as named bash functions in test-phase15.sh"
    - "All 9 new fixtures are valid JSON, repository.full_name='test-org/test-repo', minimal-but-sufficient payload shape matching GitHub webhook schema"
    - "tests/fixtures/github-issues-opened-with-pipe.json has issue.title AND issue.body containing both '|' and '\\' characters so Pitfall 1 regression fires"
    - "tests/fixtures/github-ping.json has NO 'action' field and NO 'issue' field (it is a valid GitHub ping payload)"
    - "tests/fixtures/github-push-branch-delete.json has head_commit=null so Pitfall 7 regression fires"
    - "tests/test-map.json includes a webhook/templates/ entry and includes test-phase15.sh in bin/claude-secure, webhook/, and install.sh mappings"
    - "Harness stubs claude-secure on PATH with a recorder that writes invocation argv to a log file (no real Docker)"
    - "Harness exposes setup_test_profile() that can optionally install profile prompts (override templates) so fallback-chain tests can exercise all resolve_template branches"
  artifacts:
    - path: "tests/test-phase15.sh"
      provides: "Integration+unit test harness for Phase 15 with 28 named test functions"
      contains: "test_issues_opened_routes"
    - path: "tests/fixtures/github-issues-labeled.json"
      provides: "issues.labeled payload with issue.labels=[{name:bug}] and top-level label object"
      contains: "\"action\": \"labeled\""
    - path: "tests/fixtures/github-push-feature-branch.json"
      provides: "push payload to refs/heads/feature/xyz for filter-rejection test"
      contains: "refs/heads/feature/xyz"
    - path: "tests/fixtures/github-push-bot-loop.json"
      provides: "push payload from pusher.name=claude-bot for loop_prevention test"
      contains: "claude-bot"
    - path: "tests/fixtures/github-push-branch-delete.json"
      provides: "push with head_commit=null (Pitfall 7 regression)"
      contains: "\"head_commit\": null"
    - path: "tests/fixtures/github-workflow-run-failure.json"
      provides: "workflow_run completed+failure payload for HOOK-05"
      contains: "\"conclusion\": \"failure\""
    - path: "tests/fixtures/github-workflow-run-success.json"
      provides: "workflow_run completed+success for filter rejection test"
      contains: "\"conclusion\": \"success\""
    - path: "tests/fixtures/github-workflow-run-in-progress.json"
      provides: "workflow_run in_progress for wrong-action filter test"
      contains: "\"action\": \"in_progress\""
    - path: "tests/fixtures/github-ping.json"
      provides: "ping event (Pitfall 4 regression) — no action, no issue"
      contains: "\"zen\""
    - path: "tests/fixtures/github-issues-opened-with-pipe.json"
      provides: "issue with pipe+backslash in title/body (Pitfall 1 regression)"
      contains: "|"
    - path: "tests/test-map.json"
      provides: "Updated test-to-path mapping with webhook/templates/ and test-phase15.sh entries"
      contains: "test-phase15.sh"
  key_links:
    - from: "tests/test-phase15.sh"
      to: "stub claude-secure binary on PATH"
      via: "PATH=<tmpdir>/bin:$PATH export"
      pattern: "export PATH"
    - from: "tests/test-phase15.sh"
      to: "openssl dgst -sha256 -hmac"
      via: "printf '%s' \"$BODY\" | openssl"
      pattern: "printf '%s'"
    - from: "tests/test-phase15.sh"
      to: "Phase 14 harness helpers"
      via: "inline copy of install_stub, setup_test_profile, start_listener, gen_sig"
      pattern: "start_listener"
---

<objective>
Create the Wave 0 test scaffold for Phase 15 following the exact Nyquist self-healing pattern from Phase 14 (Plan 14-01): every test function is named and defined up-front; many fail RED today and transition to GREEN as Plans 15-02, 15-03, and 15-04 land.

Purpose: Lock the 28-row test contract from 15-VALIDATION.md before any production code changes. Every HOOK-03/04/05/07 behavior and every locked decision (D-01 through D-22) is expressed as a named bash function that later plans can point their `<verify>` blocks at. Wave 0 also creates 9 new fixtures — including three regression fixtures (ping, branch-delete, pipe-in-issue) that encode the highest-risk Pitfalls 1, 4, and 7 from 15-RESEARCH.md.

Output: `tests/test-phase15.sh` (executable), 9 JSON fixtures in `tests/fixtures/`, and an updated `tests/test-map.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-event-handlers/15-CONTEXT.md
@.planning/phases/15-event-handlers/15-RESEARCH.md
@.planning/phases/15-event-handlers/15-VALIDATION.md
@.planning/phases/14-webhook-listener/14-01-PLAN.md
@.planning/phases/14-webhook-listener/14-01-SUMMARY.md
@tests/test-phase14.sh
@tests/fixtures/github-issues-opened.json
@tests/fixtures/github-push.json
@tests/test-map.json

<interfaces>
<!-- Phase 14 harness functions (tests/test-phase14.sh) reused by Phase 15: -->
<!-- Phase 15 copies (does NOT source) these for isolation. -->

install_stub()            # creates stub claude-secure on PATH; STUB_LOG env var
setup_test_profile()      # creates ~/.claude-secure/profiles/test-profile with profile.json
start_listener()          # launches webhook/listener.py as background process, stores LISTENER_PID
gen_sig(body, secret)     # printf '%s' "$body" | openssl dgst -sha256 -hmac "$secret"

<!-- Key fixture shape reference (Event Schema Reference from 15-RESEARCH.md): -->
<!-- issues: { action, issue: { number, title, body, html_url, user, labels: [{name}] }, repository, sender } -->
<!-- push:   { ref, after, head_commit: { id, message, author: {name} }, pusher: {name}, compare, repository } -->
<!-- workflow_run: { action, workflow_run: { id, conclusion, head_branch, head_sha, html_url }, workflow: { name }, repository } -->
<!-- ping:   { zen, hook_id, repository }  (NO action, NO issue) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 9 Phase 15 fixture files</name>
  <files>
    tests/fixtures/github-issues-labeled.json,
    tests/fixtures/github-push-feature-branch.json,
    tests/fixtures/github-push-bot-loop.json,
    tests/fixtures/github-push-branch-delete.json,
    tests/fixtures/github-workflow-run-failure.json,
    tests/fixtures/github-workflow-run-success.json,
    tests/fixtures/github-workflow-run-in-progress.json,
    tests/fixtures/github-ping.json,
    tests/fixtures/github-issues-opened-with-pipe.json
  </files>
  <action>
    Create 9 minimal-but-valid GitHub webhook payload fixtures. All must have `repository.full_name = "test-org/test-repo"` so they match the existing Phase 14 test profile. Follow the terse style of `tests/fixtures/github-issues-opened.json` (17 lines, only the fields the code reads).

    Fixture content requirements (from 15-RESEARCH.md "Test Fixtures Needed" table and Event Schema Reference):

    1. `github-issues-labeled.json` — `action: "labeled"`, `issue.number`, `issue.title`, `issue.body`, `issue.html_url`, `issue.user.login`, `issue.labels: [{"name": "bug"}, {"name": "triage"}]`, top-level `label: {"name": "bug"}` (the just-added label), standard `repository`, `sender`.

    2. `github-push-feature-branch.json` — identical shape to existing `github-push.json` EXCEPT `ref: "refs/heads/feature/xyz"`. Keep `head_commit` populated. This is the push-filter rejection test.

    3. `github-push-bot-loop.json` — `ref: "refs/heads/main"`, `pusher.name: "claude-bot"` (profile will list this in `webhook_bot_users`), otherwise full `head_commit`. This is the loop_prevention test.

    4. `github-push-branch-delete.json` — `ref: "refs/heads/old-feature"`, `deleted: true`, `head_commit: null` (literal JSON null, no quotes). Pitfall 7 regression — render_template must not crash on null head_commit. Include `before`, `after`, `pusher.name`, `repository`.

    5. `github-workflow-run-failure.json` — `action: "completed"`, `workflow_run: { id: 289782451, name: "", head_branch: "main", head_sha: "abc123...", conclusion: "failure", status: "completed", html_url: "https://github.com/test-org/test-repo/actions/runs/289782451" }`, top-level `workflow: { id: 123, name: "CI", path: ".github/workflows/test.yml" }` (Pitfall 6: workflow.name is the human name, not workflow_run.name), `repository`, `sender`.

    6. `github-workflow-run-success.json` — same as failure fixture BUT `workflow_run.conclusion: "success"`. Filter rejection test.

    7. `github-workflow-run-in-progress.json` — `action: "in_progress"`, `workflow_run.conclusion: null`, `workflow_run.status: "in_progress"`. Filter rejection on wrong action.

    8. `github-ping.json` — `zen: "Non-blocking is better than blocking."`, `hook_id: 12345`, `hook: { type: "Repository", id: 12345, ... }`, `repository: { full_name: "test-org/test-repo", ... }`, `sender: { login: "test-user" }`. CRITICAL: NO top-level `action` field, NO `issue`, NO `push` content. This is the Pitfall 4 regression and the D-04 "unsupported event type" test.

    9. `github-issues-opened-with-pipe.json` — `action: "opened"`, `issue.title: "fix(api): handle | in header"` (literal pipe), `issue.body: "Broken on `path\\to\\file` when body contains | pipes"` (both `|` and `\` in one body), `issue.labels: []`, `issue.user.login: "bug-reporter"`, `issue.html_url`, standard `repository`, `sender`. This fixture is the Pitfall 1 regression — loaded into a test that calls `render_template` and greps the result for the literal pipe string.

    Each file must be valid JSON (`jq -e . <file>` exits 0). Keep them as small as possible — do NOT paste GitHub's full reference payloads. Only include fields the code reads.
  </action>
  <verify>
    <automated>for f in tests/fixtures/github-issues-labeled.json tests/fixtures/github-push-feature-branch.json tests/fixtures/github-push-bot-loop.json tests/fixtures/github-push-branch-delete.json tests/fixtures/github-workflow-run-failure.json tests/fixtures/github-workflow-run-success.json tests/fixtures/github-workflow-run-in-progress.json tests/fixtures/github-ping.json tests/fixtures/github-issues-opened-with-pipe.json; do jq -e . "$f" >/dev/null || exit 1; done && jq -e '.action == "labeled" and (.issue.labels | length) >= 1 and .label.name == "bug"' tests/fixtures/github-issues-labeled.json >/dev/null && jq -e '.ref == "refs/heads/feature/xyz"' tests/fixtures/github-push-feature-branch.json >/dev/null && jq -e '.pusher.name == "claude-bot"' tests/fixtures/github-push-bot-loop.json >/dev/null && jq -e '.head_commit == null and .deleted == true' tests/fixtures/github-push-branch-delete.json >/dev/null && jq -e '.action == "completed" and .workflow_run.conclusion == "failure" and .workflow.name == "CI"' tests/fixtures/github-workflow-run-failure.json >/dev/null && jq -e '.workflow_run.conclusion == "success"' tests/fixtures/github-workflow-run-success.json >/dev/null && jq -e '.action == "in_progress"' tests/fixtures/github-workflow-run-in-progress.json >/dev/null && jq -e '.zen and (.action == null) and (.issue == null)' tests/fixtures/github-ping.json >/dev/null && jq -e '.issue.title | contains("|")' tests/fixtures/github-issues-opened-with-pipe.json >/dev/null && jq -e '.issue.body | contains("|") and contains("\\")' tests/fixtures/github-issues-opened-with-pipe.json >/dev/null</automated>
  </verify>
  <done>All 9 fixtures exist, are valid JSON, and jq assertions above pass (repository.full_name="test-org/test-repo" in all; ping has no action; branch-delete has null head_commit; pipe fixture has both `|` and `\` in body).</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test-phase15.sh with 28 named test functions</name>
  <files>tests/test-phase15.sh</files>
  <action>
    Create `tests/test-phase15.sh` as an executable bash script modeled on `tests/test-phase14.sh`. Inline-copy (not source) the helper functions: `install_stub`, `setup_test_profile`, `start_listener`, `gen_sig`, `run_test`, `cleanup` trap. Isolation is the goal — tests must run standalone.

    Set these up-front:
    - `set -uo pipefail`
    - `PASS=0; FAIL=0; TOTAL=0`
    - `PROJECT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"`
    - `TEST_TMPDIR=$(mktemp -d)` with cleanup trap
    - `LISTENER_PORT=19015` (avoid collision with Phase 14's 19000)
    - Export `STUB_LOG="$TEST_TMPDIR/stub-invocations.log"`

    Harness augmentations vs. Phase 14:
    - `setup_test_profile()` accepts optional args for extra profile fields: `webhook_event_filter` JSON blob, `webhook_bot_users` array, and a flag to pre-populate `$profile_dir/prompts/<event_type>.md` override templates for fallback-chain tests.
    - `install_prompts_override()` helper that writes a fake template to `~/.claude-secure/profiles/test-profile/prompts/<name>.md` for resolve_template tests.
    - `seed_event_file()` helper that copies a fixture into the test events dir with a synthetic `<iso>-<uuid8>.json` name (so `test_replay_*` tests can find it by substring).

    Then define ALL 28 test functions as named shell functions. Each function must grep for the content that the corresponding later plan will produce — the self-healing red→green transition. Mark each function body with a comment identifying which plan turns it green.

    Full test list (from 15-VALIDATION.md Per-Task Verification Map — each function body must actually execute something, not be a stub `return 0` or `return 1`):

    ### HOOK-03 (green via Plan 15-02):
    - `test_issues_opened_routes()` — POST `github-issues-opened.json` with valid HMAC, assert 202, assert event file written, assert top-level `event_type=issues-opened`, assert stub recorded an invocation with `--event-file` arg.
    - `test_issues_labeled_routes()` — same as opened but with labeled fixture; asserts `event_type=issues-labeled` and stub invoked.
    - `test_issues_closed_filtered()` — POST a synthesized issues-closed payload (derived from issues-opened.json with action replaced via jq); assert 202, assert NO event file, assert `webhook.jsonl` contains `event=filtered` line with `reason=issue_action_not_matched`, assert stub NOT invoked.
    - `test_default_template_issues_opened_exists()` — file existence check: `[ -f "$PROJECT_DIR/webhook/templates/issues-opened.md" ]` AND `grep -q '{{ISSUE_TITLE}}' "$PROJECT_DIR/webhook/templates/issues-opened.md"` AND `grep -q '{{ISSUE_BODY}}'` AND `grep -q '{{REPO_NAME}}'`.

    ### HOOK-04 (green via Plan 15-02):
    - `test_push_main_routes()` — POST github-push.json, assert 202, event file, stub invoked.
    - `test_push_feature_branch_filtered()` — POST github-push-feature-branch.json, assert 202, NO event file, log line `event=filtered reason=branch_not_matched:feature/xyz`.
    - `test_push_bot_loop_filtered()` — Profile configured with `webhook_bot_users=["claude-bot"]`. POST github-push-bot-loop.json. Assert 202, NO event file, log `reason=loop_prevention`.
    - `test_push_branch_delete_no_crash()` — POST github-push-branch-delete.json. Branch `old-feature` is not in allowed list → filtered. Assert 202 (not 500). No crash in listener. (This exercises render_template's null-safety indirectly — the listener must not crash parsing the payload either.)

    ### HOOK-05 (green via Plan 15-02):
    - `test_workflow_run_failure_routes()` — POST workflow-run-failure.json, assert 202, event file, stub invoked, event file has top-level `event_type=workflow_run-completed`.
    - `test_workflow_run_success_filtered()` — POST workflow-run-success.json, assert 202, NO event file, log `reason=workflow_conclusion_not_matched:success`.
    - `test_workflow_run_in_progress_filtered()` — POST workflow-run-in-progress.json, assert 202, NO event file, log `reason=workflow_action_not_completed:in_progress`.
    - `test_workflow_template_dry_run()` — Invoke the REAL bin/claude-secure (not the stub) in `--dry-run` mode against the failure fixture: `PATH="$PROJECT_DIR/bin:$PATH" claude-secure --profile test-profile spawn --event-file tests/fixtures/github-workflow-run-failure.json --dry-run`. Assert exit 0 AND stdout contains literal `CI` (WORKFLOW_NAME from .workflow.name, NOT empty — Pitfall 6 regression) AND stdout contains the workflow_run id `289782451` AND **stdout contains the literal string `main`** — this is the BRANCH fallback assertion: the failure fixture has NO `.ref` field, so BRANCH must be resolved from `.workflow_run.head_branch` (which is `main` in the fixture). If the gated `[ -s "$v_file" ]` fallback in render_template is implemented wrong, BRANCH renders as empty and this assertion fails. This grep is the execution-time tripwire for the Plan 15-03 Region 2 latent bug.

    ### HOOK-07 (green via Plan 15-03):
    - `test_replay_finds_single_match()` — seed_event_file with `20260412T120000Z-deadbeef.json` containing the issues-opened payload. Run `claude-secure replay deadbeef`. Assert exit 0 and the stub log shows a spawn invocation.
    - `test_replay_ambiguous_errors()` — seed TWO event files with substring `abcd1234` in filename. Run `claude-secure replay abcd1234`. Assert non-zero exit AND stderr contains both filenames.
    - `test_replay_no_match_errors()` — Run `claude-secure replay zzzzzzzz`. Assert non-zero exit AND stderr contains "no event file matching".
    - `test_replay_auto_profile()` — seed event file with `repository.full_name=test-org/test-repo`. Run `claude-secure replay <substring>` (no --profile flag). Assert exit 0 and stub log shows `--profile test-profile`.

    ### Locked decision assertions:
    - `test_compute_event_type_cases()` — Use `python3 -c "import sys; sys.path.insert(0, '$PROJECT_DIR/webhook'); from listener import compute_event_type; ..."` to unit-test the listener helper. Cases: `({'X-GitHub-Event':'issues'}, {'action':'opened'}) -> 'issues-opened'`; `({'X-GitHub-Event':'push'}, {}) -> 'push'`; `({'X-GitHub-Event':'ping'}, {}) -> 'ping'`; `({'X-GitHub-Event':'workflow_run'}, {'action':'completed'}) -> 'workflow_run-completed'`. Assert each case.
    - `test_ping_event_filtered()` — POST github-ping.json. Profile has valid webhook_secret → HMAC passes. Assert 202, NO event file, NO spawn, log `reason=unsupported_event:ping`.
    - `test_extract_field_truncates()` — Source `bin/claude-secure` OR invoke the helper via a small shim. Pass a 9000-byte string. Read back temp file. Assert byte length ≤ 8300 (8192 + suffix) AND file ends with `[truncated N more bytes]`.
    - `test_extract_field_utf8_safe()` — Pass a string of exactly 8190 ASCII bytes followed by a 3-byte UTF-8 codepoint (e.g. `$'\xE2\x98\x83'` — snowman). Assert no partial-codepoint cut in output file (decode as UTF-8 without error).
    - `test_render_handles_pipe_in_value()` — Invoke `claude-secure spawn --dry-run --event-file github-issues-opened-with-pipe.json`. Assert stdout contains the literal string `fix(api): handle | in header` (i.e. pipe preserved, no sed truncation).
    - `test_render_handles_backslash_in_value()` — Same invocation. Assert stdout contains literal `path\to\file` or similar backslash from the body — no sed escape misinterpretation.
    - `test_resolve_template_fallback_chain()` — Set up a profile WITHOUT `prompts/issues-opened.md`. Assert that `--dry-run` against an issues-opened fixture still renders via the repo's `webhook/templates/issues-opened.md` fallback. Assert `WEBHOOK_TEMPLATES_DIR=` env set to `$PROJECT_DIR/webhook/templates` is respected.
    - `test_explicit_template_no_default_fallback()` — Set up a profile WITHOUT `prompts/nonsense.md`. Invoke `claude-secure spawn --prompt-template nonsense --event-file ...`. Assert non-zero exit and stderr contains "Template not found" — explicit flag must NOT fall back to defaults (D-13 step 1 rule).
    - `test_webhook_templates_dir_env_var()` — Create `$TEST_TMPDIR/custom-templates/issues-opened.md` with unique marker content. Export `WEBHOOK_TEMPLATES_DIR=$TEST_TMPDIR/custom-templates`. Run `--dry-run` against issues-opened fixture with a profile that has NO prompts/ override. Assert stdout contains the unique marker from the custom dir.
    - `test_install_copies_templates_dir()` — Grep contract against `install.sh`: `grep -q 'webhook/templates' install.sh` AND `grep -q 'mkdir -p /opt/claude-secure/webhook/templates' install.sh`. Green after Plan 15-04.
    - `test_event_file_has_top_level_event_type()` — POST github-issues-opened.json with valid HMAC. Assert the persisted event file JSON has BOTH `.event_type == "issues-opened"` AND `._meta.event_type == "issues-opened"`.
    - `test_spawn_event_type_priority()` — This test owns its setup. Inside the test body: (1) call `setup_test_profile` to ensure the profile exists; (2) create a custom profile-level template at `~/.claude-secure/profiles/test-profile/prompts/priority-top.md` whose only substantive content is the literal line `EVENT_TYPE={{EVENT_TYPE}}` (plus optional header — the `EVENT_TYPE=` prefix is the grep anchor, not a token); (3) synthesize an event file under `$CONFIG_DIR/events/` where `.event_type="priority-top"`, `._meta.event_type="priority-mid"`, and `.action="priority-bot"`, plus `.repository.full_name="test-org/test-repo"` so profile resolution works; (4) invoke `PATH="$PROJECT_DIR/bin:$PATH" claude-secure --profile test-profile spawn --event-file <path> --prompt-template priority-top --dry-run`. Assert exit 0 AND stdout contains the literal string `EVENT_TYPE=priority-top` (proving do_spawn's jq chain `.event_type // ._meta.event_type // .action` picked the top-level field, NOT `priority-mid` or `priority-bot`). The `--prompt-template priority-top` flag is required because no default template carries an `EVENT_TYPE={{EVENT_TYPE}}` anchor line — the test explicitly provides the template it needs.

    At the bottom, add a dispatcher:
    ```bash
    if [ $# -gt 0 ]; then
      "$@"
      exit $?
    fi

    run_test test_issues_opened_routes test_issues_opened_routes
    run_test test_issues_labeled_routes test_issues_labeled_routes
    # ... (all 28 functions) ...

    echo "=============================="
    echo "Phase 15: $PASS/$TOTAL passed, $FAIL failed"
    [ $FAIL -eq 0 ]
    ```

    Wave 0 expectation: Plans 15-02 and 15-03 are NOT landed yet, so MANY tests will fail. That is the Nyquist self-healing contract (see Phase 14 Plan 01 precedent). The `<automated>` verify for THIS plan only checks that the script exists, is executable, parses with `bash -n`, and produces output — not that all assertions pass.

    File permissions: `chmod +x tests/test-phase15.sh`.
  </action>
  <verify>
    <automated>test -x tests/test-phase15.sh && bash -n tests/test-phase15.sh && grep -q 'test_issues_opened_routes' tests/test-phase15.sh && grep -q 'test_issues_labeled_routes' tests/test-phase15.sh && grep -q 'test_issues_closed_filtered' tests/test-phase15.sh && grep -q 'test_default_template_issues_opened_exists' tests/test-phase15.sh && grep -q 'test_push_main_routes' tests/test-phase15.sh && grep -q 'test_push_feature_branch_filtered' tests/test-phase15.sh && grep -q 'test_push_bot_loop_filtered' tests/test-phase15.sh && grep -q 'test_push_branch_delete_no_crash' tests/test-phase15.sh && grep -q 'test_workflow_run_failure_routes' tests/test-phase15.sh && grep -q 'test_workflow_run_success_filtered' tests/test-phase15.sh && grep -q 'test_workflow_run_in_progress_filtered' tests/test-phase15.sh && grep -q 'test_workflow_template_dry_run' tests/test-phase15.sh && grep -q 'test_replay_finds_single_match' tests/test-phase15.sh && grep -q 'test_replay_ambiguous_errors' tests/test-phase15.sh && grep -q 'test_replay_no_match_errors' tests/test-phase15.sh && grep -q 'test_replay_auto_profile' tests/test-phase15.sh && grep -q 'test_compute_event_type_cases' tests/test-phase15.sh && grep -q 'test_ping_event_filtered' tests/test-phase15.sh && grep -q 'test_extract_field_truncates' tests/test-phase15.sh && grep -q 'test_extract_field_utf8_safe' tests/test-phase15.sh && grep -q 'test_render_handles_pipe_in_value' tests/test-phase15.sh && grep -q 'test_render_handles_backslash_in_value' tests/test-phase15.sh && grep -q 'test_resolve_template_fallback_chain' tests/test-phase15.sh && grep -q 'test_explicit_template_no_default_fallback' tests/test-phase15.sh && grep -q 'test_webhook_templates_dir_env_var' tests/test-phase15.sh && grep -q 'test_install_copies_templates_dir' tests/test-phase15.sh && grep -q 'test_event_file_has_top_level_event_type' tests/test-phase15.sh && grep -q 'test_spawn_event_type_priority' tests/test-phase15.sh && grep -q 'priority-top' tests/test-phase15.sh && grep -q 'EVENT_TYPE=priority-top' tests/test-phase15.sh && grep -q "printf '%s'" tests/test-phase15.sh && grep -q 'install_stub' tests/test-phase15.sh && grep -q 'LISTENER_PORT=19015' tests/test-phase15.sh</automated>
  </verify>
  <done>tests/test-phase15.sh exists, is executable, parses clean with bash -n, contains all 28 named test functions, uses printf '%s' (not echo) for HMAC, defines install_stub, binds to port 19015 (not 19000), test_workflow_template_dry_run greps stdout for `main` (BRANCH fallback tripwire), and test_spawn_event_type_priority creates its own `prompts/priority-top.md` template with an `EVENT_TYPE={{EVENT_TYPE}}` anchor line and asserts stdout contains `EVENT_TYPE=priority-top`.</done>
</task>

<task type="auto">
  <name>Task 3: Update tests/test-map.json with Phase 15 entries</name>
  <files>tests/test-map.json</files>
  <action>
    Update `tests/test-map.json` to include Phase 15:

    1. Add a new top-level mapping: `{ "paths": ["webhook/templates/"], "tests": ["test-phase15.sh"] }` so changes to default templates trigger Phase 15 tests.

    2. Update the existing `bin/claude-secure` mapping to include `test-phase15.sh` in its tests array (alongside test-phase6, 12, 13, 14).

    3. Update the existing `webhook/` mapping to include `test-phase15.sh` in its tests array.

    4. Update the existing `install.sh` mapping to include `test-phase15.sh` in its tests array (alongside test-phase4, 12, 14).

    5. Add a self-referential mapping: `{ "paths": ["tests/test-phase15.sh"], "tests": ["test-phase15.sh"] }`.

    Keep the `always_skip` block unchanged. Keep ordering predictable (additions go at the end of the relevant cluster; the self-referential mapping goes next to the other `tests/test-phase*.sh` entries).

    The resulting JSON must validate with `jq -e . tests/test-map.json` and must satisfy the grep assertions in verify.
  </action>
  <verify>
    <automated>jq -e . tests/test-map.json >/dev/null && jq -e '.mappings[] | select(.paths[0] == "webhook/templates/") | .tests | index("test-phase15.sh")' tests/test-map.json >/dev/null && jq -e '.mappings[] | select(.paths[0] == "bin/claude-secure") | .tests | index("test-phase15.sh")' tests/test-map.json >/dev/null && jq -e '.mappings[] | select(.paths[0] == "webhook/") | .tests | index("test-phase15.sh")' tests/test-map.json >/dev/null && jq -e '.mappings[] | select(.paths[0] == "install.sh") | .tests | index("test-phase15.sh")' tests/test-map.json >/dev/null && jq -e '.mappings[] | select(.paths[0] == "tests/test-phase15.sh") | .tests | index("test-phase15.sh")' tests/test-map.json >/dev/null</automated>
  </verify>
  <done>tests/test-map.json parses as valid JSON; webhook/templates/ mapping exists pointing at test-phase15.sh; bin/claude-secure, webhook/, install.sh, and tests/test-phase15.sh mappings all list test-phase15.sh.</done>
</task>

</tasks>

<verification>
After all three tasks, run:

  bash -n tests/test-phase15.sh                      # syntax check
  jq -e . tests/test-map.json                        # JSON valid
  for f in tests/fixtures/github-*.json; do jq -e . "$f" >/dev/null || exit 1; done
  test -x tests/test-phase15.sh                      # executable

Do NOT run `bash tests/test-phase15.sh` as a gate — many assertions will fail RED (that is the intended Wave 0 state).

Phase 13 and Phase 14 regression suites must still pass because this plan only adds new files:

  bash tests/test-phase13.sh
  bash tests/test-phase14.sh

Both suites must remain green.
</verification>

<success_criteria>
- [ ] 9 new fixtures exist and are valid JSON with the per-fixture field assertions in Task 1 verify
- [ ] tests/test-phase15.sh exists, is executable, parses with bash -n, contains all 28 named test functions
- [ ] tests/test-phase15.sh inlines the Phase 14 harness helpers (install_stub, setup_test_profile, start_listener, gen_sig)
- [ ] tests/test-phase15.sh uses `printf '%s'` (not `echo`) for HMAC body generation — Gotcha 2 from Phase 14 research
- [ ] tests/test-phase15.sh binds to LISTENER_PORT=19015 (not 19000) to avoid Phase 14 collision
- [ ] test_workflow_template_dry_run asserts stdout contains `main` (BRANCH fallback tripwire for the Plan 15-03 Region 2 gated-substitution fix)
- [ ] test_spawn_event_type_priority creates its own profile-level `prompts/priority-top.md` template with `EVENT_TYPE={{EVENT_TYPE}}` anchor and asserts stdout contains `EVENT_TYPE=priority-top`
- [ ] tests/test-map.json includes webhook/templates/ mapping and test-phase15.sh entries on bin/claude-secure, webhook/, install.sh
- [ ] Phase 13 test suite still green (no edits to production code)
- [ ] Phase 14 test suite still green (no edits to production code)
</success_criteria>

<output>
After completion, create `.planning/phases/15-event-handlers/15-01-SUMMARY.md` following the standard summary template, documenting:
- Every fixture created and its purpose
- The 28 test functions present in the new test file
- The self-healing red→green contract (most tests fail until Plans 15-02/15-03/15-04 land — this is intentional)
- Confirmation that Phase 13 and Phase 14 test suites still pass
</output>
</output>
