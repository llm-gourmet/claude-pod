---
phase: 26-stop-hook-mandatory-reporting
plan: 03
type: execute
wave: 2
depends_on:
  - "26-01"
  - "26-02"
files_modified:
  - bin/claude-secure
autonomous: true
requirements:
  - SPOOL-03
must_haves:
  truths:
    - "run_spool_shipper() function exists in bin/claude-secure, forks _spool_shipper_loop to background with disown"
    - "run_spool_shipper() returns in < 100ms even when the publish_docs_bundle path would take 5+ seconds"
    - "On successful publish, spool.md is deleted"
    - "On 3 failed publish attempts, spool-audit.jsonl gets a push_failed entry with attempt=3; spool.md is retained for next spawn's drain"
    - "run_spool_shipper_inline() exists for stale-drain use at spawn preamble (Plan 04 uses it)"
    - "CLAUDE_SECURE_SKIP_SPOOL_SHIPPER=1 short-circuits run_spool_shipper to a no-op (test escape hatch)"
    - "run_spool_shipper is called from do_spawn headless path after write_audit_entry"
    - "run_spool_shipper is called from the interactive *) dispatch case after docker compose exec -it returns"
  artifacts:
    - path: "bin/claude-secure"
      provides: "run_spool_shipper, run_spool_shipper_inline, _spool_shipper_loop, _spool_audit_write functions + two integration call sites"
      contains: "run_spool_shipper"
  key_links:
    - from: "bin/claude-secure:do_spawn"
      to: "run_spool_shipper"
      via: "function call after write_audit_entry (~line 2253)"
      pattern: "run_spool_shipper"
    - from: "bin/claude-secure:interactive-case"
      to: "run_spool_shipper"
      via: "function call after docker compose exec -it (~line 2850)"
      pattern: "run_spool_shipper"
    - from: "run_spool_shipper"
      to: "publish_docs_bundle"
      via: "subshell + disown, 3-attempt jittered retry loop"
      pattern: "publish_docs_bundle"
---

<objective>
Implement the host-side async spool shipper that reads $LOG_DIR/spool.md, calls publish_docs_bundle with jittered retry, and writes audit entries on success/failure — without ever blocking the caller. Wire it into both spawn paths (headless do_spawn and interactive `*)` dispatch).

Purpose: SPOOL-03 (a host-side async shipper reads the spool after Claude exits and pushes to the doc repo with jittered backoff — failure is logged to audit JSONL and never blocks the next spawn).

Output: Four new bash functions added to bin/claude-secure (run_spool_shipper, run_spool_shipper_inline, _spool_shipper_loop, _spool_audit_write) plus two call-site insertions (one in do_spawn around line 2253, one in the interactive dispatch around line 2850).

Turns RED tests 10-14 from Plan 01 into GREEN:
- test_run_spool_shipper_function_exists
- test_shipper_returns_immediately
- test_shipper_deletes_spool_on_success
- test_shipper_logs_push_failed_with_attempt
- test_shipper_publishes_malformed_best_effort

Key implementation notes (from 26-RESEARCH.md Pitfall 2):
- MUST use `( _spool_shipper_loop ... ) & disown` — the subshell + disown combo fully detaches. Forgetting disown means spawn_cleanup trap may reap the background job.
- Subshell stdout MUST be redirected to /dev/null to avoid interleave with the envelope emit at do_spawn:2255.
- The shipper calls publish_docs_bundle which internally already calls verify_bundle_sections — do NOT call verify_bundle_sections separately (D-04 best-effort: broken reports publish anyway).
- Audit writes go to a SEPARATE file ($LOG_DIR/${LOG_PREFIX}spool-audit.jsonl), NOT executions.jsonl (Pitfall 4 — avoid O_APPEND collision).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-stop-hook-mandatory-reporting/26-CONTEXT.md
@.planning/phases/26-stop-hook-mandatory-reporting/26-RESEARCH.md
@.planning/phases/26-stop-hook-mandatory-reporting/26-01-SUMMARY.md
@.planning/phases/26-stop-hook-mandatory-reporting/26-02-SUMMARY.md

# Existing helpers the shipper wraps (read for signature + stdout contract)
@bin/claude-secure

@tests/test-phase26.sh

<interfaces>
<!-- publish_docs_bundle signature (from bin/claude-secure:1658 — read the file for exact details) -->
<!-- Caller contract documented at line 1657:
     "stdout last line = report URL (for HTTPS) or rel path (for file://)" -->

publish_docs_bundle $body_path $session_id $summary_line $delivery_id
  → stdout: diagnostic output + last line = report URL
  → exit 0 on success, non-zero on failure
  → requires globals: PROFILE, DOCS_REPO, DOCS_REPO_TOKEN (or REPORT_REPO_TOKEN via Phase 23 alias),
    DOCS_PROJECT_DIR, DOCS_BRANCH, CONFIG_DIR, LOG_DIR

<!-- Integration point line numbers (verified in 26-RESEARCH.md — Existing Code Analysis) -->
do_spawn entry: bin/claude-secure:1975
Headless docker exec (claude call): bin/claude-secure:2159
Audit write: bin/claude-secure:2238-2252
Envelope emit: bin/claude-secure:2255
Return: bin/claude-secure:2258
Interactive *) exec: bin/claude-secure:2850

<!-- Session ID sources (from 26-CONTEXT.md D-05) -->
Headless: $_audit_session (extracted at bin/claude-secure:2205)
Interactive: generate with `$(uuidgen 2>/dev/null | tr '[:upper:]' '[:lower:]' | tr -d - || echo "manual-$$")`

<!-- Testability contracts from Plan 01 (tests/test-phase26.sh) -->
CLAUDE_SECURE_SKIP_SPOOL_SHIPPER: if "1", run_spool_shipper is a no-op (returns 0)
MOCK_PUBLISH_BUNDLE_EXIT: tests source a stub `publish_docs_bundle() { ... }` before sourcing bin/claude-secure;
  the test harness reads this env var to determine mock exit code

<!-- Existing patterns to follow -->
write_audit_entry: bin/claude-secure:1209 (canonical audit writer — use separate spool-audit.jsonl, not executions.jsonl)
push_with_retry: bin/claude-secure:1290 (3-attempt pattern we mirror, but at a higher level)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add run_spool_shipper family of functions to bin/claude-secure</name>
  <files>bin/claude-secure</files>
  <read_first>
    - bin/claude-secure lines 1657-1760 (publish_docs_bundle signature + stdout contract)
    - bin/claude-secure lines 1209-1285 (write_audit_entry — pattern for jq -cn audit writes)
    - bin/claude-secure lines 1290-1360 (push_with_retry — retry pattern we mirror)
    - .planning/phases/26-stop-hook-mandatory-reporting/26-RESEARCH.md (Implementation Approach section 3 — function sketches; Pitfalls 2, 3, 4)
    - tests/test-phase26.sh (shipper test assertions — MOCK_PUBLISH_BUNDLE_EXIT contract, spool-audit.jsonl path expectations)
  </read_first>
  <behavior>
    - Four new functions added to bin/claude-secure in a contiguous block placed immediately before `publish_docs_bundle()` (~line 1655):
      1. `run_spool_shipper` — background fork entrypoint
      2. `run_spool_shipper_inline` — synchronous drain entrypoint (Plan 04 uses this for D-07 stale drain)
      3. `_spool_shipper_loop` — shared retry body
      4. `_spool_audit_write` — audit JSONL writer for spool events
    - run_spool_shipper:
      - Takes one positional arg: session_id
      - Honors CLAUDE_SECURE_SKIP_SPOOL_SHIPPER=1 as early return (test escape hatch — per D-05 / Claude's Discretion)
      - Resolves $spool_file from $LOG_DIR/spool.md
      - Returns 0 if spool_file does not exist (silent skip — no reporting configured, per D-05)
      - Forks: `( _spool_shipper_loop "$spool_file" "$session_id" >/dev/null 2>&1 ) & disown` — both the &>/dev/null redirect AND disown are mandatory (Pitfalls 2 + stdout interleave)
      - Returns 0 within < 100ms of the call
    - run_spool_shipper_inline:
      - Same signature, but runs `_spool_shipper_loop` in the current shell (blocking)
      - Used by Plan 04's stale-drain call site at spawn preamble
      - Drain failures MUST NOT crash the caller — always return 0 at the top level (leave spool on failure for next time; see Open Question 3)
    - _spool_shipper_loop:
      - 3-attempt loop
      - Attempt 1: delay 0s; Attempt 2: delay 5±2s; Attempt 3: delay 10±2s (bash $RANDOM-based jitter per 26-RESEARCH.md sketch)
      - Each attempt: `report_url=$(publish_docs_bundle "$spool_file" "$session_id" "$summary" "$delivery_id" 2>&1 | tail -1)`. If exit 0 → _spool_audit_write "pushed" "$attempt" "$report_url" "" ; rm -f "$spool_file" ; return 0
      - After 3 failures: _spool_audit_write "push_failed" "$attempt" "" "publish_docs_bundle rc=$rc (attempts: 3)" ; return 1
      - summary line: `Phase 26 spool: ${session_id}` (fallback placeholder; the real summary lives inside the bundle body)
      - delivery_id: use the passed session_id (publish_docs_bundle accepts either per 26-RESEARCH.md note)
    - _spool_audit_write:
      - Takes 4 args: status, attempt, report_url, error
      - Writes to `$LOG_DIR/${LOG_PREFIX:-}spool-audit.jsonl` (SEPARATE from executions.jsonl per Pitfall 4)
      - Uses jq -cn with: ts (UTC ISO8601), profile (from $PROFILE), spool_status, attempt, report_url (omitted if empty), error (omitted if empty, truncated to 200 chars)
      - Always silent on write failures (append `|| true`) — audit writes must not crash the shipper
  </behavior>
  <action>
    Use the Edit tool to insert the four new functions into bin/claude-secure immediately before the `publish_docs_bundle()` function definition (which is at line ~1658 per 26-RESEARCH.md).

    The insertion MUST be a single contiguous block placed BEFORE publish_docs_bundle so that the shipper functions are visible to any caller of publish_docs_bundle (source order matters in bash).

    Copy the implementation sketch from 26-RESEARCH.md "Implementation Approach → 3. `run_spool_shipper()` + `_spool_shipper_loop()`" section verbatim, with these clarifications:

    1. The shipper subshell line MUST be: `( _spool_shipper_loop "$spool_file" "$session_id" >/dev/null 2>&1 ) & disown`
       — both `>/dev/null 2>&1` AND `disown` are required. The redirect prevents interleave with the envelope emit at do_spawn:2255 (Open Question 4 answer). The disown prevents spawn_cleanup trap from reaping the child (Pitfall 2 + Open Question 2).

    2. Add a header comment block citing "Phase 26 SPOOL-03: fork-and-disown async shipper" for future grep-ability.

    3. The `_spool_shipper_loop` retry loop uses `while [ "$attempt" -lt "$max_attempts" ]` (attempt 0→3, pre-increment). Delay computation:
       ```bash
       local base=$(( attempt * 5 ))  # 0, 5, 10 (computed BEFORE increment)
       local jitter=$(( (RANDOM % 5) - 2 ))  # -2..+2
       local delay=$(( base + jitter ))
       [ "$delay" -lt 0 ] && delay=0
       sleep "$delay"
       ```
       Only sleep if attempt < max_attempts (don't sleep after the last failed attempt).

    4. Capture publish_docs_bundle output robustly — if `publish_docs_bundle` fails, the `$(... | tail -1)` still returns the last stderr/stdout line, but bash's `$?` is the tail exit code (always 0). Pitfall-avoid: use `set -o pipefail` locally OR capture exit via a temp file. Recommended pattern:
       ```bash
       local _tmpout="$(mktemp)"
       if publish_docs_bundle "$spool_file" "$session_id" "$summary" "$delivery_id" >"$_tmpout" 2>&1; then
         report_url=$(tail -1 "$_tmpout")
         rm -f "$_tmpout"
         _spool_audit_write "pushed" "$attempt" "$report_url" ""
         rm -f "$spool_file"
         return 0
       fi
       rc=$?
       rm -f "$_tmpout"
       ```
       This is the SAFE version of the 26-RESEARCH.md sketch — it correctly captures the publish exit code without relying on pipefail (which may not be set globally).

    5. The `CLAUDE_SECURE_SKIP_SPOOL_SHIPPER=1` short-circuit goes at the TOP of run_spool_shipper (before spool_file resolution) so tests can fully suppress the shipper.

    6. `run_spool_shipper_inline` shares the same body as run_spool_shipper but calls `_spool_shipper_loop` directly (no subshell, no disown). It is used by Plan 04 at spawn preamble to drain stale spools synchronously. Its return value at the top level is always 0 (drain failures are logged but do not crash the caller — answer to Open Question 3).

    7. `_spool_audit_write` uses `jq -cn` (compact) and `--arg`/`--argjson` for safe value injection. The error field MUST be truncated via `${error:0:200}` to prevent JSONL line-length explosions.

    DO NOT modify publish_docs_bundle itself. DO NOT change its signature.
    DO NOT add call sites in this task (Task 2 adds them) — this task is function definition only.
  </action>
  <verify>
    <automated>bash -n bin/claude-secure &amp;&amp; grep -c 'run_spool_shipper()' bin/claude-secure | grep -qx 1 &amp;&amp; grep -c '_spool_shipper_loop()' bin/claude-secure | grep -qx 1 &amp;&amp; grep -c 'run_spool_shipper_inline()' bin/claude-secure | grep -qx 1 &amp;&amp; grep -c '_spool_audit_write()' bin/claude-secure | grep -qx 1 &amp;&amp; grep -q 'CLAUDE_SECURE_SKIP_SPOOL_SHIPPER' bin/claude-secure &amp;&amp; grep -q '&amp; disown' bin/claude-secure &amp;&amp; grep -q 'spool-audit.jsonl' bin/claude-secure &amp;&amp; bash tests/test-phase26.sh test_run_spool_shipper_function_exists 2&gt;&amp;1 | grep -q PASS &amp;&amp; bash tests/test-phase26.sh test_shipper_deletes_spool_on_success 2&gt;&amp;1 | grep -q PASS &amp;&amp; bash tests/test-phase26.sh test_shipper_logs_push_failed_with_attempt 2&gt;&amp;1 | grep -q PASS &amp;&amp; bash tests/test-phase26.sh test_shipper_returns_immediately 2&gt;&amp;1 | grep -q PASS &amp;&amp; bash tests/test-phase26.sh test_shipper_publishes_malformed_best_effort 2&gt;&amp;1 | grep -q PASS</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n bin/claude-secure` (exit 0 — syntax valid)
    - `grep -q '^run_spool_shipper()' bin/claude-secure` (exit 0)
    - `grep -q '^run_spool_shipper_inline()' bin/claude-secure` (exit 0)
    - `grep -q '^_spool_shipper_loop()' bin/claude-secure` (exit 0)
    - `grep -q '^_spool_audit_write()' bin/claude-secure` (exit 0)
    - `grep -q 'CLAUDE_SECURE_SKIP_SPOOL_SHIPPER' bin/claude-secure` (exit 0)
    - `grep -q '& disown' bin/claude-secure` (exit 0 — fork-and-disown pattern)
    - `grep -q 'spool-audit.jsonl' bin/claude-secure` (exit 0 — separate audit file per Pitfall 4)
    - `bash tests/test-phase26.sh test_run_spool_shipper_function_exists 2>&1 | grep -q PASS` (exit 0)
    - `bash tests/test-phase26.sh test_shipper_deletes_spool_on_success 2>&1 | grep -q PASS` (exit 0)
    - `bash tests/test-phase26.sh test_shipper_logs_push_failed_with_attempt 2>&1 | grep -q PASS` (exit 0)
    - `bash tests/test-phase26.sh test_shipper_returns_immediately 2>&1 | grep -q PASS` (exit 0)
    - `bash tests/test-phase26.sh test_shipper_publishes_malformed_best_effort 2>&1 | grep -q PASS` (exit 0)
  </acceptance_criteria>
  <done>
    The four shipper functions are defined in bin/claude-secure before publish_docs_bundle, the file is bash-valid, CLAUDE_SECURE_SKIP_SPOOL_SHIPPER test hook is honored, the disown pattern is present, audit writes target spool-audit.jsonl (not executions.jsonl), and all 5 shipper unit tests in test-phase26.sh pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire run_spool_shipper into do_spawn and interactive spawn paths</name>
  <files>bin/claude-secure</files>
  <read_first>
    - bin/claude-secure lines 2159-2260 (do_spawn post-claude-exit flow: exec → validate → audit → envelope → return)
    - bin/claude-secure lines 2830-2870 (interactive *) dispatch path)
    - .planning/phases/26-stop-hook-mandatory-reporting/26-RESEARCH.md (Integration Points in do_spawn section + Interactive path + Open Question 4)
    - .planning/phases/26-stop-hook-mandatory-reporting/26-CONTEXT.md (D-05, D-06)
  </read_first>
  <behavior>
    - do_spawn: `run_spool_shipper "$_audit_session"` is called AFTER the `write_audit_entry` call (line ~2253) and BEFORE the envelope emit (line ~2255). Placement order: audit write → spool shipper fork → envelope emit → return.
    - Interactive path: `run_spool_shipper "<generated-session-id>"` is called AFTER the `docker compose exec -it claude claude --dangerously-skip-permissions` line (~line 2850). Session id is generated inline with: `$(uuidgen 2>/dev/null | tr '[:upper:]' '[:lower:]' | tr -d - || echo "manual-$$")`.
    - Both call sites are under a comment `# Phase 26 SPOOL-03: fork-and-disown spool shipper` for future grep-ability.
    - The do_spawn call uses `$_audit_session` (the session_id extracted from claude's stdout at line 2205) per 26-RESEARCH.md recommendation (not $delivery_id, which is webhook-scoped).
    - No changes to the spawn preamble / stale drain in this plan — Plan 04 handles D-07.
    - No changes to existing audit writes, envelope construction, or return logic — purely additive.
  </behavior>
  <action>
    Use the Edit tool to insert two call sites in bin/claude-secure.

    Call site 1 — do_spawn headless path:
      - Locate the line containing `write_audit_entry \` inside do_spawn (~line 2238-2252). This is the line that writes executions.jsonl with the final audit payload.
      - AFTER the write_audit_entry block finishes (its closing quoted arg list), BEFORE the envelope emit to stdout at ~line 2255, insert:
        ```bash
        # Phase 26 SPOOL-03: fork-and-disown spool shipper after audit write.
        # Uses $_audit_session (Claude session_id from line ~2205), not $delivery_id.
        run_spool_shipper "${_audit_session:-unknown}"
        ```
      - The `${_audit_session:-unknown}` fallback guards against an unset variable if Claude's output was malformed (defensive; the function itself silent-skips if spool.md doesn't exist).

    Call site 2 — interactive *) dispatch:
      - Locate the line containing `docker compose exec -it claude claude --dangerously-skip-permissions` (~line 2850).
      - AFTER that line (on the next logical line, before any existing cleanup), insert:
        ```bash
        # Phase 26 SPOOL-03: fork-and-disown spool shipper after interactive exit.
        run_spool_shipper "$(uuidgen 2>/dev/null | tr '[:upper:]' '[:lower:]' | tr -d - || echo "manual-$$")"
        ```

    Verify placements by reading the diff:
    - do_spawn: run_spool_shipper appears between write_audit_entry and the envelope echo/return
    - interactive: run_spool_shipper appears immediately after the docker compose exec -it line and before any cleanup trap firing

    DO NOT reorder or delete existing lines. DO NOT modify write_audit_entry, the envelope construction, or cleanup logic. Both insertions are single new-line additions with a comment header.
  </action>
  <verify>
    <automated>bash -n bin/claude-secure &amp;&amp; grep -c 'run_spool_shipper ' bin/claude-secure | awk '$1 &gt;= 2 {exit 0} {exit 1}' &amp;&amp; grep -B2 'run_spool_shipper' bin/claude-secure | grep -q 'Phase 26 SPOOL-03' &amp;&amp; awk '/write_audit_entry/{found=NR} /run_spool_shipper "\${_audit_session/{if(NR&gt;found) exit 0} END{exit 1}' bin/claude-secure &amp;&amp; awk '/docker compose exec -it claude claude/{found=NR} /run_spool_shipper "\$\(uuidgen/{if(NR&gt;found) exit 0} END{exit 1}' bin/claude-secure</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n bin/claude-secure` (exit 0 — syntax still valid after edits)
    - `[ "$(grep -c 'run_spool_shipper ' bin/claude-secure)" -ge 2 ]` (exit 0 — both call sites present; matches the trailing-space form of the function call, excluding definitions)
    - `grep -B1 'run_spool_shipper "${_audit_session' bin/claude-secure | grep -q 'Phase 26 SPOOL-03'` (exit 0 — do_spawn call site has the comment header)
    - `grep -B1 'run_spool_shipper "$(uuidgen' bin/claude-secure | grep -q 'Phase 26 SPOOL-03'` (exit 0 — interactive call site has the comment header)
    - `awk '/write_audit_entry/{found=NR} /run_spool_shipper "\${_audit_session/{if(NR>found) exit 0} END{exit 1}' bin/claude-secure` (exit 0 — do_spawn run_spool_shipper call appears AFTER write_audit_entry)
    - `awk '/docker compose exec -it claude claude/{found=NR} /run_spool_shipper "\$\(uuidgen/{if(NR>found) exit 0} END{exit 1}' bin/claude-secure` (exit 0 — interactive call appears AFTER the exec -it line)
    - `bash tests/test-phase26.sh 2>&1 | grep -E '^(PASS|FAIL):' | wc -l` is at least 14 (exit 0 — all registered tests still run)
    - Full Phase 24 and Phase 25 test suites still pass: `bash tests/test-phase24.sh >/dev/null 2>&1 && bash tests/test-phase25.sh >/dev/null 2>&1` (exit 0 — regression check; no contract breakage)
  </acceptance_criteria>
  <done>
    bin/claude-secure contains two new run_spool_shipper call sites, both preceded by the "Phase 26 SPOOL-03" comment, placed correctly in do_spawn (after write_audit_entry) and the interactive dispatch (after docker compose exec -it). Syntax is valid, Phase 24 and Phase 25 tests still pass (no regression), and all Phase 26 shipper tests now pass.
  </done>
</task>

</tasks>

<verification>
- `bash -n bin/claude-secure` (syntax check)
- `bash tests/test-phase26.sh` passes all stop-hook tests (from Plan 02) AND all shipper tests (from this plan): tests 3-14 GREEN
- Only test 15 (stale drain) remains RED — Plan 04 addresses it
- Phase 24 and Phase 25 regression tests still pass
- `grep -c 'run_spool_shipper ' bin/claude-secure` >= 2 (two call sites)
- `grep -c 'run_spool_shipper()' bin/claude-secure` == 1 (one definition)
- `grep -c 'run_spool_shipper_inline()' bin/claude-secure` == 1 (one definition, no call site yet)
- No changes outside bin/claude-secure
</verification>

<success_criteria>
- SPOOL-03 implemented: async shipper reads spool.md after Claude exits, calls publish_docs_bundle with jittered retry, never blocks spawn return
- Four functions added: run_spool_shipper, run_spool_shipper_inline, _spool_shipper_loop, _spool_audit_write
- Two call sites added (headless + interactive)
- Background fork uses `( ... >/dev/null 2>&1 ) & disown` pattern (Pitfall 2 avoided)
- Audit writes target spool-audit.jsonl (separate file per Pitfall 4)
- Test escape hatch CLAUDE_SECURE_SKIP_SPOOL_SHIPPER honored
- All shipper unit tests pass
- No regression in Phase 24 / Phase 25 test suites
</success_criteria>

<output>
After completion, create `.planning/phases/26-stop-hook-mandatory-reporting/26-03-SUMMARY.md` documenting:
- Line numbers where the four functions were inserted
- Exact line numbers of the two call sites (before-edit / after-edit)
- Shipper retry/jitter math with worked example
- Test results: shipper tests now GREEN, Phase 24/25 regression status
- Remaining RED test (stale drain) for Plan 04
</output>
