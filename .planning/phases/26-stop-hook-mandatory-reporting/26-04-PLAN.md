---
phase: 26-stop-hook-mandatory-reporting
plan: 04
type: execute
wave: 3
depends_on:
  - "26-01"
  - "26-02"
  - "26-03"
files_modified:
  - bin/claude-secure
  - README.md
autonomous: true
requirements:
  - SPOOL-01
  - SPOOL-03
must_haves:
  truths:
    - "run_spool_shipper_inline is called from do_spawn preamble (headless) before fetch_docs_context"
    - "run_spool_shipper_inline is called from the interactive *) dispatch preamble after mkdir -p $LOG_DIR"
    - "A stale spool.md left over from a previous crashed session is drained before the new session starts"
    - "Drain failures never abort the new spawn (answer to Open Question 3)"
    - "README.md documents the Stop hook + spool path + what to do when a spool is stuck"
    - "Full test-phase26.sh suite is GREEN"
    - "Phase 24 and Phase 25 regression tests still pass"
  artifacts:
    - path: "bin/claude-secure"
      provides: "Stale-spool drain call sites at spawn preamble (headless + interactive)"
      contains: "run_spool_shipper_inline"
    - path: "README.md"
      provides: "Operator-facing Phase 26 documentation: mandatory reporting, spool path, stuck-spool troubleshooting"
      contains: "Phase 26"
  key_links:
    - from: "bin/claude-secure:do_spawn-preamble"
      to: "run_spool_shipper_inline"
      via: "inline synchronous drain call before fetch_docs_context"
      pattern: "run_spool_shipper_inline"
    - from: "bin/claude-secure:interactive-preamble"
      to: "run_spool_shipper_inline"
      via: "inline synchronous drain call after mkdir -p LOG_DIR"
      pattern: "run_spool_shipper_inline"
    - from: "README.md"
      to: "tests/test-phase26.sh"
      via: "operator documentation references spool.md path and shipper audit JSONL"
      pattern: "spool.md"
---

<objective>
Close the SPOOL enforcement loop: add the D-07 stale-spool drain at spawn preamble (both headless and interactive paths) so a previous crashed session's leftover spool is pushed to the doc repo before the new session begins. Also document Phase 26 for operators in README.md.

Purpose: SPOOL-01 / SPOOL-03 edge case (Success criterion #4 of Phase 26: a stuck stale spool must NOT silently block new sessions forever — it must be drained at next spawn start). Plus operator-facing documentation so users know where the spool lives and how to recover from a stuck shipper.

Output: Two new run_spool_shipper_inline call sites in bin/claude-secure plus a README.md Phase 26 section.

This is the final plan for Phase 26. After this plan lands, the full tests/test-phase26.sh suite should be GREEN.

Key constraints:
- The drain call goes at spawn PREAMBLE (before containers start, before fetch_docs_context), not at exit.
- Drain failures MUST NOT crash the new spawn (answer to Open Question 3 from 26-RESEARCH.md). The leftover spool is left in place for the NEXT next-spawn to retry.
- README section uses operator prose with no D-IDs leaked (mirror Phase 16/17 README style from Plan 16-04 and Plan 17-04 notes in STATE.md).
- No changes to Dockerfile, settings.json, docker-compose.yml, or tests/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-stop-hook-mandatory-reporting/26-CONTEXT.md
@.planning/phases/26-stop-hook-mandatory-reporting/26-RESEARCH.md
@.planning/phases/26-stop-hook-mandatory-reporting/26-01-SUMMARY.md
@.planning/phases/26-stop-hook-mandatory-reporting/26-02-SUMMARY.md
@.planning/phases/26-stop-hook-mandatory-reporting/26-03-SUMMARY.md

@bin/claude-secure
@README.md
@tests/test-phase26.sh

<interfaces>
Functions available from Plan 03:
  run_spool_shipper_inline SESSION_ID
    - runs _spool_shipper_loop synchronously (blocking)
    - always returns 0 at top level (drain failures logged but do not crash caller)
    - silent no-op if LOG_DIR/spool.md does not exist (common case)

Integration points (verified in 26-RESEARCH.md):
  Headless preamble: BEFORE fetch_docs_context (bin/claude-secure:2109), AFTER load_profile_config has set LOG_DIR
  Interactive preamble: AFTER mkdir -p LOG_DIR (bin/claude-secure:2836), BEFORE cleanup_containers (bin/claude-secure:2839)

Session ID generation (drain calls):
  $(uuidgen 2>/dev/null | tr '[:upper:]' '[:lower:]' | tr -d - || echo "drain-$$")

README structure reference (from STATE.md Phase 16-04 note):
  Phase 26 section goes AFTER the Phase 17 section and BEFORE the Testing section.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add stale-spool drain call sites to bin/claude-secure spawn preamble</name>
  <files>bin/claude-secure</files>
  <read_first>
    - bin/claude-secure lines 2090-2160 (do_spawn preamble: load_profile_config, fetch_docs_context, docker compose up)
    - bin/claude-secure lines 2820-2860 (interactive star-dispatch preamble)
    - .planning/phases/26-stop-hook-mandatory-reporting/26-RESEARCH.md (Stale Spool Drain Integration Point section + Open Question 3)
    - .planning/phases/26-stop-hook-mandatory-reporting/26-03-SUMMARY.md (line numbers for Plan 03 insertions)
    - tests/test-phase26.sh test_stale_spool_drained_at_spawn_preamble assertion
  </read_first>
  <behavior>
    - do_spawn headless preamble: run_spool_shipper_inline is called AFTER load_profile_config has run (LOG_DIR set) and BEFORE fetch_docs_context starts. This ensures any stale spool from a prior crashed session is drained synchronously before the new session begins.
    - Interactive star-dispatch preamble: same call inserted AFTER mkdir -p LOG_DIR (line ~2836) and BEFORE any cleanup or container start.
    - Both call sites use a generated session id with a drain-PID fallback per 26-RESEARCH.md sketch.
    - Both call sites are under a comment "Phase 26 D-07: drain any leftover spool from a prior crashed session".
    - Drain call is suffixed with " || true" defensively, even though the function itself always returns 0.
    - Phase 24 and Phase 25 tests still pass after this change.
    - Plan 03 call sites (after write_audit_entry, after interactive exec -it) are preserved unchanged.
  </behavior>
  <action>
    Use the Edit tool to add two insertions in bin/claude-secure.

    Insertion 1 — do_spawn headless preamble:
      - Locate the fetch_docs_context call in do_spawn (~line 2109 per 26-RESEARCH.md). This is AFTER load_profile_config (earlier in do_spawn) and BEFORE the container work starts.
      - Insert IMMEDIATELY BEFORE the fetch_docs_context line a 3-line block:
        (line 1) # Phase 26 D-07: drain any leftover spool from a prior crashed session.
        (line 2) # Synchronous, non-fatal — failures are logged but do not abort the new spawn.
        (line 3) run_spool_shipper_inline "$(uuidgen 2>/dev/null | tr '[:upper:]' '[:lower:]' | tr -d - || echo "drain-$$")" || true

    Insertion 2 — interactive star-dispatch preamble:
      - Locate the mkdir -p "$LOG_DIR" line in the top-level star-dispatch case (~line 2836).
      - Insert IMMEDIATELY AFTER that line and BEFORE cleanup_containers (~line 2839) a 2-line block:
        (line 1) # Phase 26 D-07: drain any leftover spool from a prior crashed session.
        (line 2) run_spool_shipper_inline "$(uuidgen 2>/dev/null | tr '[:upper:]' '[:lower:]' | tr -d - || echo "drain-$$")" || true

    Verify:
    - Neither insertion touches Plan 03's existing run_spool_shipper call sites (those are at POST-Claude-exit, not preamble).
    - Both insertions are additive — no lines deleted.
    - bash -n bin/claude-secure still passes.
    - Phase 24/25 regression tests still pass.

    DO NOT use run_spool_shipper (the backgrounded form) here — the preamble drain MUST be synchronous so the new session can rely on the spool path being clear.
  </action>
  <verify>
    <automated>bash -n bin/claude-secure &amp;&amp; [ "$(grep -c 'run_spool_shipper_inline ' bin/claude-secure)" -ge 2 ] &amp;&amp; bash tests/test-phase26.sh test_stale_spool_drained_at_spawn_preamble 2&gt;&amp;1 | grep -q PASS &amp;&amp; bash tests/test-phase24.sh &gt;/dev/null 2&gt;&amp;1 &amp;&amp; bash tests/test-phase25.sh &gt;/dev/null 2&gt;&amp;1</automated>
  </verify>
  <acceptance_criteria>
    - bash -n bin/claude-secure (exit 0 — syntax still valid)
    - grep -c 'run_spool_shipper_inline ' bin/claude-secure is at least 2 (two call sites present)
    - grep -c 'run_spool_shipper_inline()' bin/claude-secure is exactly 1 (definition count unchanged from Plan 03)
    - grep -B1 'run_spool_shipper_inline ' bin/claude-secure | grep -q 'Phase 26 D-07' (exit 0 — both call sites have comment header)
    - awk '/run_spool_shipper_inline/{ins=NR} /fetch_docs_context/{if(ins && NR-ins<=3) {print "ok"; exit 0}} END{exit 1}' bin/claude-secure (exit 0 — inline call precedes fetch_docs_context by 1-3 lines)
    - bash tests/test-phase26.sh test_stale_spool_drained_at_spawn_preamble 2>&1 | grep -q PASS (exit 0)
    - bash tests/test-phase24.sh >/dev/null 2>&1 (exit 0 — no Phase 24 regression)
    - bash tests/test-phase25.sh >/dev/null 2>&1 (exit 0 — no Phase 25 regression)
    - Plan 03 call sites preserved: grep -c 'run_spool_shipper ' bin/claude-secure is at least 2 (not counting run_spool_shipper_inline — the trailing space distinguishes them)
  </acceptance_criteria>
  <done>
    bin/claude-secure contains two new run_spool_shipper_inline call sites at spawn preamble (headless + interactive), both with the Phase 26 D-07 comment header, placed correctly before fetch_docs_context and after mkdir -p LOG_DIR. Syntax is valid, test_stale_spool_drained_at_spawn_preamble passes, Phase 24/25 suites still pass, Plan 03 call sites are preserved unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document Phase 26 in README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (current structure — find Phase 17 section and Testing section to identify insertion point)
    - .planning/phases/26-stop-hook-mandatory-reporting/26-CONTEXT.md (D-01, D-03, D-05, D-07 for operator-facing facts: spool path, re-prompt behavior, shipper audit file, stale drain)
    - .planning/STATE.md Phase 16-04 / Phase 17-04 notes (README placement pattern — "between X and Testing as the operator observability anchor")
  </read_first>
  <behavior>
    - README.md gains a new "Mandatory Reporting (Phase 26)" section.
    - Section is placed AFTER the existing Phase 17 / Operational Hardening section and BEFORE the Testing section (mirror the pattern from Plans 16-04 and 17-04 per STATE.md notes).
    - Content uses operator-facing prose — no D-IDs (D-01, D-07 etc.) leaked, no decision-table references.
    - Covers 4 facts:
      1. Every Claude session writes /var/log/claude-secure/spool.md before exit; Stop hook re-prompts once if missing.
      2. After Claude exits, the host-side shipper pushes the spool to the profile's doc repo with 3-attempt jittered retry, then deletes the spool on success.
      3. On failure, the shipper leaves the spool in place and logs to LOG_DIR/spool-audit.jsonl; the next spawn drains it at preamble.
      4. Stuck-spool troubleshooting: operator can tail the audit JSONL to see push_failed entries and inspect the spool file directly.
    - Includes a copy-pasteable jq command for watching the audit file.
    - Does NOT reference the Stop hook API schema, the stop_hook_active field, or internal function names.
    - The existing README structure (headings, code block style, tone) is preserved.
  </behavior>
  <action>
    Read README.md to find:
    1. The Phase 17 or "Operational Hardening" section end
    2. The Testing section start

    Use the Edit tool to insert a new markdown section between them. Approximate content template:

    ---

    ## Mandatory Reporting

    Every claude-secure session — headless (webhook-triggered) and interactive — writes a session report to the doc repo bound to its profile. Reports are enforced by a local Stop hook and shipped by a host-side async process.

    ### How it works

    When Claude finishes a turn, a Stop hook runs inside the container and checks for a local spool file at /var/log/claude-secure/spool.md (which maps to $LOG_DIR/spool.md on the host). If the file is missing, Claude is re-prompted exactly once with a template listing the six required sections: Goal, Where Worked, What Changed, What Failed, How to Test, Future Findings. If the second attempt still fails, the hook yields so Claude is never trapped in a loop.

    The Stop hook makes zero network calls — a doc repo outage cannot block Claude from exiting.

    After Claude exits, claude-secure forks a background shipper that reads spool.md, runs it through the existing redaction + sanitization + publish_docs_bundle pipeline, and pushes the resulting commit to the profile's docs_repo. The shipper uses 3-attempt jittered backoff. On success, spool.md is deleted. On failure, spool.md is left in place and the failure is logged.

    ### Operator files

    - `$LOG_DIR/spool.md` — the current session's pending report (exists briefly between Claude exit and shipper success)
    - `$LOG_DIR/${LOG_PREFIX}spool-audit.jsonl` — JSONL log of shipper outcomes: pushed, push_failed, stale_drained, with timestamps, retry count, and report URL

    ### Stuck spool recovery

    If a shipper exhausts its retries (for example, during a sustained GitHub outage), the spool file remains in $LOG_DIR. The next time you run claude-secure with the same profile, the spawn preamble drains the stale spool synchronously before starting the new session — so a transient outage self-heals.

    To inspect shipper outcomes live:

    ```bash
    tail -f $LOG_DIR/${LOG_PREFIX}spool-audit.jsonl | jq -c '{ts, status: .spool_status, attempt, error}'
    ```

    To manually push a stuck spool without waiting for the next spawn, simply re-run any spawn against the same profile — the preamble drain will pick it up.

    ---

    Adapt the code block variables ($LOG_DIR, ${LOG_PREFIX}) to match the notation used elsewhere in the existing README.

    Validate the result:
    - The new section appears AFTER Phase 17 / Operational Hardening content and BEFORE Testing
    - Existing headings and links are not moved or renumbered
    - No D-IDs leak into the prose
  </action>
  <verify>
    <automated>grep -q '## Mandatory Reporting' README.md &amp;&amp; grep -q 'spool.md' README.md &amp;&amp; grep -q 'spool-audit.jsonl' README.md &amp;&amp; ! grep -qE 'D-0[1-9]|SPOOL-0[1-3]' README.md &amp;&amp; awk '/Mandatory Reporting/{mr=NR} /## Testing/{if(mr &amp;&amp; NR&gt;mr) exit 0} END{exit 1}' README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -q '## Mandatory Reporting' README.md (exit 0 — section heading present)
    - grep -q '/var/log/claude-secure/spool.md' README.md (exit 0 — container path documented)
    - grep -q 'spool-audit.jsonl' README.md (exit 0 — audit file documented)
    - grep -q 'six required sections' README.md OR grep -q 'Goal, Where Worked' README.md (exit 0 — section template referenced)
    - ! grep -qE 'D-0[1-9]\b' README.md (exit 0 — no decision-ID leakage in the new section; check with grep of the appended region)
    - ! grep -q 'SPOOL-0[1-3]' README.md (exit 0 — no requirement-ID leakage)
    - awk '/## Mandatory Reporting/{mr=NR} /## Testing/{if(mr && NR>mr) exit 0} END{exit 1}' README.md (exit 0 — Mandatory Reporting section precedes Testing)
    - grep -q 'stop_hook_active' README.md is FALSE (operator docs do not leak internal API field names)
  </acceptance_criteria>
  <done>
    README.md has a new "## Mandatory Reporting" section placed between the operational hardening content and the Testing section. The section documents the spool file path, shipper retry behavior, spool-audit.jsonl observability file, and stuck-spool recovery via stale drain. No D-IDs, SPOOL-IDs, or internal API field names leak into the prose. A copy-pasteable tail+jq command is provided for live shipper observability.
  </done>
</task>

</tasks>

<verification>
- bash -n bin/claude-secure (syntax check after preamble insertions)
- bash tests/test-phase26.sh passes ALL tests (full suite GREEN — the first time Phase 26 achieves this)
- Phase 24 and Phase 25 regression tests still pass
- README.md contains the new Mandatory Reporting section between Operational Hardening and Testing
- grep -c 'run_spool_shipper_inline ' bin/claude-secure >= 2 (two preamble call sites)
- grep -c 'run_spool_shipper_inline()' bin/claude-secure == 1 (definition unchanged from Plan 03)
- grep -c 'run_spool_shipper ' bin/claude-secure >= 2 (two Plan 03 call sites preserved; trailing space excludes the _inline variant)
- No changes to claude/, docker-compose.yml, tests/, or the Dockerfile
</verification>

<success_criteria>
- SPOOL-01 edge case closed: stale-spool drain at spawn preamble ensures crashed-session leftovers reach the doc repo on the next spawn
- SPOOL-03 robustness verified: drain failures log to audit JSONL but never crash the new spawn (Open Question 3 answered in code)
- Full tests/test-phase26.sh suite is GREEN — 15+ tests all pass
- Phase 24 and Phase 25 regression suites still pass (no contract breakage)
- README.md gives operators a self-sufficient view of the mandatory reporting mechanism: spool path, audit JSONL, stuck-spool recovery command
- Plan 03 integration call sites are preserved byte-for-byte
</success_criteria>

<output>
After completion, create `.planning/phases/26-stop-hook-mandatory-reporting/26-04-SUMMARY.md` documenting:
- Line numbers of the two new preamble call sites
- README.md section diff summary
- Full tests/test-phase26.sh test results (should be all GREEN)
- Phase 24/25 regression results (should be GREEN)
- Phase 26 closure summary: SPOOL-01, SPOOL-02, SPOOL-03 status + manual-only verification list from 26-VALIDATION.md pending operator sign-off
</output>
