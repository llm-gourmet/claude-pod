---
phase: 11-milestone-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-map.json
  - .planning/REQUIREMENTS.md
  - validator/validator.py
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05]

must_haves:
  truths:
    - "test-map.json triggers logging tests when bin/claude-secure changes"
    - "test-map.json triggers compose-dependent tests when docker-compose.yml changes"
    - "test-map.json triggers whitelist env-file test when config/whitelist.json changes"
    - "REQUIREMENTS.md shows TEST-01 through TEST-05 as Complete"
    - "/validate endpoint docstring describes it as debug/observability-only"
  artifacts:
    - path: "tests/test-map.json"
      provides: "Complete test coverage mappings"
      contains: "test-phase6.sh"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table with TEST requirements marked Complete"
      contains: "Complete"
    - path: "validator/validator.py"
      provides: "Documented /validate endpoint"
      contains: "debug"
  key_links:
    - from: "tests/test-map.json"
      to: "git-hooks/pre-push"
      via: "pre-push hook reads test-map.json for smart test selection"
      pattern: "test-map.json"
---

<objective>
Close v1.0 audit gaps: fix test-map.json coverage holes, mark TEST requirements complete in REQUIREMENTS.md, and document /validate endpoint as debug-only.

Purpose: Finalize milestone cleanup so all audit items are resolved and test coverage mapping is accurate.
Output: Updated test-map.json, REQUIREMENTS.md, and validator.py with documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/test-map.json
@.planning/REQUIREMENTS.md
@validator/validator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix test-map.json coverage gaps</name>
  <files>tests/test-map.json</files>
  <read_first>
    - tests/test-map.json
  </read_first>
  <action>
Update tests/test-map.json to close three coverage gaps:

1. **bin/claude-secure mapping**: Currently maps only to `["test-phase9.sh"]`. The CLI wrapper also has logging flags tested by test-phase6.sh. Update to:
   ```json
   { "paths": ["bin/claude-secure"], "tests": ["test-phase6.sh", "test-phase9.sh"] }
   ```

2. **docker-compose.yml mapping**: Currently maps only to `["test-phase1.sh"]`. Compose changes affect logging (phase 6), env-file (phase 7), and multi-instance (phase 9). Update to:
   ```json
   { "paths": ["docker-compose.yml"], "tests": ["test-phase1.sh", "test-phase6.sh", "test-phase7.sh", "test-phase9.sh"] }
   ```

3. **config/whitelist.json mapping**: Currently maps to `["test-phase1.sh", "test-phase3.sh"]`. Whitelist changes also affect env-file strategy tests (phase 7). Update to:
   ```json
   { "paths": ["config/whitelist.json"], "tests": ["test-phase1.sh", "test-phase3.sh", "test-phase7.sh"] }
   ```

Keep all other mappings unchanged. Preserve the `always_skip` array as-is.
  </action>
  <verify>
    <automated>cd /home/igor9000/claude-secure && jq '.mappings[] | select(.paths[] == "bin/claude-secure") | .tests' tests/test-map.json | grep -q 'test-phase6.sh' && jq '.mappings[] | select(.paths[] == "docker-compose.yml") | .tests | length' tests/test-map.json | grep -q '4' && jq '.mappings[] | select(.paths[] == "config/whitelist.json") | .tests | length' tests/test-map.json | grep -q '3' && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.mappings[] | select(.paths[] == "bin/claude-secure") | .tests' tests/test-map.json` contains both "test-phase6.sh" and "test-phase9.sh"
    - `jq '.mappings[] | select(.paths[] == "docker-compose.yml") | .tests | length' tests/test-map.json` outputs 4
    - `jq '.mappings[] | select(.paths[] == "docker-compose.yml") | .tests' tests/test-map.json` contains "test-phase1.sh", "test-phase6.sh", "test-phase7.sh", "test-phase9.sh"
    - `jq '.mappings[] | select(.paths[] == "config/whitelist.json") | .tests | length' tests/test-map.json` outputs 3
    - `jq '.mappings[] | select(.paths[] == "config/whitelist.json") | .tests' tests/test-map.json` contains "test-phase7.sh"
    - All other mappings unchanged
    - File is valid JSON: `jq . tests/test-map.json` exits 0
  </acceptance_criteria>
  <done>test-map.json has complete coverage: bin/claude-secure triggers phase 6+9 tests, docker-compose.yml triggers phase 1+6+7+9 tests, config/whitelist.json triggers phase 1+3+7 tests</done>
</task>

<task type="auto">
  <name>Task 2: Mark TEST requirements complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - tests/test-map.json
  </read_first>
  <action>
The TEST-01 through TEST-05 requirements describe integration tests that already exist across the test suite. Verify each is covered, then update REQUIREMENTS.md:

**Requirement verification:**
- TEST-01 (direct outbound connections blocked): Covered by test-phase1.sh (DOCK-04 DNS blocked test) and test-phase2.sh (iptables default deny)
- TEST-02 (proxy reaches Anthropic): Covered by test-phase3.sh (proxy forwarding with mock upstream) and test-phase1.sh (proxy connectivity)
- TEST-03 (secrets redacted from proxy outbound): Covered by test-phase3.sh (SECR-02 redaction test)
- TEST-04 (calls without call-ID blocked by iptables): Covered by test-phase2.sh (CALL-07 iptables enforcement)
- TEST-05 (hook blocks payloads to non-whitelisted): Covered by test-phase2.sh (CALL-03 block test)

**Changes to make in the Testing section (lines 52-56):**
Replace the unchecked boxes with checked boxes:
```
- [x] **TEST-01**: Integration test verifies that direct outbound connections from the claude container are blocked
- [x] **TEST-02**: Integration test verifies that traffic through the proxy reaches Anthropic successfully
- [x] **TEST-03**: Integration test verifies that known secrets are redacted from proxy outbound traffic
- [x] **TEST-04**: Integration test verifies that calls without valid call-ID registration are blocked by iptables
- [x] **TEST-05**: Integration test verifies that the PreToolUse hook blocks payloads to non-whitelisted domains
```

**Changes to make in the Traceability table (lines 148-152):**
Replace "Pending" with "Complete" for all five TEST rows:
```
| TEST-01 | Phase 11 (was Phase 5) | Complete |
| TEST-02 | Phase 11 (was Phase 5) | Complete |
| TEST-03 | Phase 11 (was Phase 5) | Complete |
| TEST-04 | Phase 11 (was Phase 5) | Complete |
| TEST-05 | Phase 11 (was Phase 5) | Complete |
```

**Update the coverage summary (around line 172):**
Change satisfied count: `- v1 requirements: 41 total` stays the same but ensure all 41 are mapped and the last-updated date changes to 2026-04-11.

Add after `*Last updated:` line:
```
*Last updated: 2026-04-11 after Phase 11 TEST requirements verified complete*
```
  </action>
  <verify>
    <automated>cd /home/igor9000/claude-secure && grep -c '\[x\] \*\*TEST-0' .planning/REQUIREMENTS.md | grep -q '5' && grep 'TEST-01' .planning/REQUIREMENTS.md | grep -q 'Complete' && grep 'TEST-05' .planning/REQUIREMENTS.md | grep -q 'Complete' && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '\[ \] \*\*TEST' .planning/REQUIREMENTS.md` returns no matches (no unchecked TEST items)
    - `grep '\[x\] \*\*TEST' .planning/REQUIREMENTS.md` returns 5 matches
    - `grep 'TEST-01.*Complete' .planning/REQUIREMENTS.md` returns a match
    - `grep 'TEST-02.*Complete' .planning/REQUIREMENTS.md` returns a match
    - `grep 'TEST-03.*Complete' .planning/REQUIREMENTS.md` returns a match
    - `grep 'TEST-04.*Complete' .planning/REQUIREMENTS.md` returns a match
    - `grep 'TEST-05.*Complete' .planning/REQUIREMENTS.md` returns a match
    - `grep 'Pending' .planning/REQUIREMENTS.md` returns no matches
    - `grep '2026-04-11' .planning/REQUIREMENTS.md` returns a match
  </acceptance_criteria>
  <done>All five TEST requirements (TEST-01 through TEST-05) are marked [x] in the requirements list and "Complete" in the traceability table, with no "Pending" entries remaining</done>
</task>

<task type="auto">
  <name>Task 3: Document /validate endpoint as debug-only</name>
  <files>validator/validator.py</files>
  <read_first>
    - validator/validator.py
  </read_first>
  <action>
Update the docstring of the `_handle_validate` method in `validator/validator.py` (currently at line 306) to document it as debug/observability-only.

Replace the current docstring:
```python
"""GET /validate?call_id=X -- check if a call-ID is valid and mark used."""
```

With:
```python
"""GET /validate?call_id=X -- debug/observability endpoint.

        Check if a call-ID is valid and mark it as used. This endpoint is
        NOT part of the critical security path -- iptables rules are the
        enforcement layer. This exists for debugging and observability:
        operators can query it to inspect call-ID state without affecting
        the security model.

        Returns {"valid": true/false, "domain": "..."} as JSON.
        """
```

This is a docstring-only change. Do NOT modify any logic in the method.
  </action>
  <verify>
    <automated>cd /home/igor9000/claude-secure && grep -q 'debug/observability endpoint' validator/validator.py && grep -q 'NOT part of the critical security path' validator/validator.py && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'debug/observability endpoint' validator/validator.py` returns a match
    - `grep 'NOT part of the critical security path' validator/validator.py` returns a match
    - `grep 'iptables rules are the' validator/validator.py` returns a match
    - `grep 'debugging and observability' validator/validator.py` returns a match
    - No logic changes in _handle_validate: `grep 'def _handle_validate' validator/validator.py` still exists
    - Method still queries SQLite and returns JSON: `grep 'valid.*True' validator/validator.py` still exists
  </acceptance_criteria>
  <done>/validate endpoint docstring clearly states it is debug/observability-only and NOT part of the critical security enforcement path</done>
</task>

</tasks>

<verification>
All three audit gaps closed:
1. test-map.json has no coverage holes for bin/claude-secure, docker-compose.yml, or config/whitelist.json
2. REQUIREMENTS.md shows all 41 v1 requirements as Complete with no Pending entries
3. validator.py /validate endpoint is documented as debug-only
</verification>

<success_criteria>
- `jq . tests/test-map.json` is valid JSON with expanded mappings
- `grep -c 'Pending' .planning/REQUIREMENTS.md` returns 0
- `grep -c 'Complete' .planning/REQUIREMENTS.md` returns 41 (all requirements)
- `grep 'debug/observability' validator/validator.py` returns a match
</success_criteria>

<output>
After completion, create `.planning/phases/11-milestone-cleanup/11-01-SUMMARY.md`
</output>
